# CONVENTIONS for Phase 0 plan authors (read fully before writing anything)

## Environment facts
- Today: 2026-09-05. Repo `divysinghvi/site` is EMPTY (no commits). Branch: `claude/divy-dev-observability-r8dzwg`.
- Local toolchain available: Go 1.24.7, Node 22.22, npm 10.9, Docker 29. No promtool installed (obtain via `go run github.com/prometheus/prometheus/cmd/promtool@latest` or the `prom/prometheus` image).
- Outbound HTTPS works (through a proxy). Use ToolSearch to load `WebFetch`/`WebSearch` and verify protocol claims against OFFICIAL docs (prometheus.io, grafana.com/docs, jaegertracing.io, opentelemetry.io, docs.github.com, pypistats.org, pkg.go.dev, svelte.dev, tailwindcss.com). Record every verified claim with its URL in your facts file. Mark anything you could not verify as `UNVERIFIED:`.

## What Phase 0 is
Phase 0 = a PLAN ONLY. No code. Deliverable: `docs/PLAN.md` in the repo containing: repo layout, data schemas (spans, logs, samples, postmortems + the rest of content/), the exact PromQL subset, the exact LogQL subset, an API contract table, phase deliverable checklists, open questions (max 5 total across the whole plan — so be stingy; prefer stating an assumption over asking).

## Writing style (the user demanded: "Be direct and concrete; no filler")
- Markdown. Tables for anything tabular. Fenced code for schemas/examples/commands. No marketing prose, no "we will leverage", no restating the brief.
- Every schema shown as a concrete example (YAML/JSON/SQL) plus a field table (name, type, required, meaning, constraints).
- Every unknown fact about Divy = `TODO(divy)` placeholder, never invented. Do NOT invent dates, PR numbers, URLs, counts. Dates the brief gives at month precision are written as `YYYY-MM` (schema allows `YYYY`, `YYYY-MM`, `YYYY-MM-DD`; a `precision` is implied by the string length).
- Commands must be single-paste runnable.
- Do not mention model names or AI assistants anywhere in the plan.

## Baseline decisions already taken (adopt these; do not re-litigate — if you believe one is wrong, say so in `open_questions` with a one-line reason and still write the plan assuming it)
1. Layout: exactly the brief's tree (`web/`, `api/`, `collector/` as a Go package inside the api module at `api/internal/collector` with the top-level `collector/` dir holding ONLY a README pointer? NO — keep it simple: top-level `collector/` is NOT a separate module; the Go module lives at `api/` and the collector code is `api/internal/collector/`. Keep a top-level `collector/README.md` explaining that, so the brief's tree is honored) + `content/`, `deploy/`, `docs/`, `schema/` (generated JSON Schema, committed), `Makefile`, `.github/workflows/`, `.env.example`, `README.md`.
2. Single Go module at `api/` (`module divy.dev/api`), Go 1.24, `go-chi/chi/v5`, `modernc.org/sqlite`, OpenTelemetry Go SDK (`go.opentelemetry.io/otel`, `otelhttp`), `golang.org/x/time/rate` for token buckets, `gopkg.in/yaml.v3`, `github.com/invopop/jsonschema` (struct → JSON Schema), `github.com/santhosh-tekuri/jsonschema/v6` (validate content against schema at startup and in CI), `github.com/prometheus/client_golang` for /metrics exposition + process metrics. One binary `divy` with subcommands: `serve [--collect]`, `collect [--once]`, `validate`, `schemagen`, `migrate`, `export-ascii`. Verify exact import paths / current versions on pkg.go.dev.
3. Frontend delivery (RECOMMENDED, open question #1 for the user): SvelteKit 2 + Svelte 5 runes + TypeScript + Tailwind v4, `@sveltejs/adapter-static` with full prerender (all routes are known at build time from content served by the API), and the built `web/build` directory is EMBEDDED into the Go binary via `embed.FS`, so production = one container (`api`) + Caddy (TLS/HSTS/compression). The Go server does content negotiation on `/` (`Accept: text/plain` → ASCII trace). Alternative if the user prefers true SSR: `adapter-node` as a second container. The plan must state what changes under the alternative in ≤10 lines.
   Build cycle: the Dockerfile builds the Go binary once without web assets (`-tags noweb`), runs it in the web-build stage as the content source for prerendering, then builds the final binary with the embedded assets. Dev loop: Vite dev server on :5173 proxying `/api`, `/metrics`, `/loki`, `/healthz`, `/readyz`, `/favicon.svg`, `/og`, `/robots.txt` to the Go server on :8080.
4. Type safety pipeline: Go structs in `api/internal/model` are the single source of truth → `divy schemagen` writes `schema/*.schema.json` → `web` runs `json-schema-to-typescript` to produce `web/src/lib/api/types.gen.ts` → CI fails on drift (`make gen && git diff --exit-code`). Content files are validated against the same schemas by `divy validate` (also run in CI and at `serve` startup; startup fails fast on invalid content).
5. Time series storage: SQLite (WAL, `busy_timeout=5000`, single writer goroutine). Tables (names fixed): `schema_migrations`, `series` (id, metric, labels canonical-JSON, unique(metric,labels)), `samples` (series_id, ts_ms INTEGER, value REAL, PK(series_id, ts_ms)) WITHOUT ROWID, `probe_results` (target, ts_ms, up, latency_ms, status_code, error), `otel_spans` (trace_id, span_id, parent_span_id, name, service, start_unix_nano, end_unix_nano, attributes JSON, events JSON, status_code, status_msg), `collector_runs` (collector, started_ms, finished_ms, ok, error, items). Timestamps: integer unix milliseconds in `samples`/`probe_results`; nanoseconds in `otel_spans`. Retention: samples 2y, probe_results raw 90d, otel_spans 24h or last 20k spans (whichever smaller), enforced by the collector loop.
6. Counter semantics: `*_total` metrics are stored as CUMULATIVE values (Prometheus counter semantics). Collectors that see history (GitHub contribution calendar: daily counts for the last 365 days; GitHub merged PRs: full list with mergedAt; pypistats overall: last 180 days daily) BACKFILL cumulative daily samples on every run (upsert, so retroactive changes are absorbed). Gauges with no history (stars, open PRs) start at first collection and the plan says so. Cadence: GitHub every 15m, PyPI every 60m, uptime probes every 5m, retention/rollups every 60m. All cadences and the GitHub token come from env (`.env.example` lists them).
7. Metric catalogue — final names (promtool lint compliant; note `github_stars_total` from the brief is renamed to `github_stars` because promlint rejects `_total` on gauges; say this explicitly in the plan):
   - `github_commits_total` (counter; labels: none) — from contributionsCollection calendar (all contributions counted as commits? NO: use `totalCommitContributions` per day is not available per-day; use contributionCalendar daily `contributionCount` and name the metric honestly: keep `github_commits_total` but document that it counts GitHub "contributions" (commits+PRs+issues+reviews) per GitHub's calendar; verify in docs whether a commits-only per-day series is obtainable and prefer it if so).
   - `github_merged_prs_total{org}` (counter) — from GitHub search `is:pr is:merged author:divysinghvi`; `org` = repo owner login; for repos whose owner is in `GITHUB_PRIVATE_ORGS` (env, default `gradr`) or whose repo is private, only the count is stored (never repo names). Also `github_merged_prs_by_repo_total{org,repo}` for PUBLIC repos only.
   - `github_stars{repo}` (gauge), `github_followers` (gauge).
   - `pypi_downloads_total{package}` (counter, from pypistats.org overall, mirrors=false), `pypi_package_info{package,version}` (gauge=1, from pypi.org JSON API).
   - `savely_active_users` (gauge, source=manual, from `content/manual_metrics.yaml`, with companion `divy_manual_metric_updated_timestamp_seconds{metric}`).
   - `oss_prs_open` (gauge, GitHub search `is:pr is:open author:divysinghvi -user:divysinghvi`), `lfx_applications{status}` (gauge, manual).
   - `divy_uptime_seconds` (gauge, process uptime), `divy_build_info{version,commit,go_version}` (gauge=1), `divy_open_to_work` (gauge 1/0 from `content/profile.yaml`; used by the DivyAvailableForHire alert).
   - Uptime probes use blackbox_exporter names: `probe_success{target}`, `probe_duration_seconds{target}`, `probe_http_status_code{target}`.
   - `divy_collector_last_success_timestamp_seconds{collector}`, `divy_collector_runs_total{collector,result}`.
   - Plus standard `go_*`/`process_*` and `divy_http_requests_total{route,method,code}` / `divy_http_request_duration_seconds` histogram from the API itself.
   `/metrics` exposes the LATEST sample of every stored series (plus live process metrics). `/api/v1/query_range` reads history from SQLite.
8. Trace identity: the career trace has a FIXED trace id = hex of the first 16 bytes of sha256("divy.career"); span ids = first 8 bytes of sha256(span.id). `/api/traces/career` is an alias. Request self-traces use real OTel ids from the SDK and are stored in `otel_spans` by a custom SpanExporter; the same `/api/traces/{id}` endpoint resolves both.
9. Linking: log lines carry an optional `span` field (a span id from `content/spans.yaml`); postmortems carry `span` in frontmatter; `divy validate` fails on dangling references. `GET /api/content/todos` lists every `TODO(divy)` found across `content/` (file, path, text) so the user can track what to fill.
10. Rate limiting: token bucket per client IP (`X-Forwarded-For` trusted only from Caddy's IP range / `TRUSTED_PROXIES` env), 20 req/s sustained, burst 100, 429 + `Retry-After`. `/healthz`, `/readyz`, `/metrics` exempt from the bucket but capped globally. Responses to `/api/v1/*`, `/loki/*`, `/api/content/*` carry `Cache-Control` (public, max-age 15–60s), ETag; an in-memory response cache keyed by normalized query for 15s.
11. Alerts: `content/alerts.yaml` in Prometheus rule-file shape (`groups[].rules[]` with `alert`, `expr`, `for`, `labels`, `annotations`). `GET /api/v1/rules` serves them in Prometheus API shape. The web app evaluates them client-side by polling `/api/v1/query` every 15s and tracking pending→firing per `for`. `DivyAvailableForHire` uses `expr: divy_open_to_work == 1`, `for: 30s`.
12. Panels: `content/panels.yaml` defines the dashboard (panel id, title, type, targets[].expr, legendFormat, unit, thresholds, gridPos{x,y,w,h}, description, source note). The URL hash stores only layout overrides + time range.
13. Uptime targets: `content/uptime.yaml` (id, name, url, method, expected_status, timeout, interval). Unknown URLs are `TODO(divy)` and the collector SKIPS targets whose url is a TODO (reported as "unconfigured", never green).
14. Every API JSON error uses Prometheus' error envelope `{"status":"error","errorType":"bad_data","error":"..."}` on `/api/v1/*` and Loki's on `/loki/*`; everything else uses `{"error":"..."}`.
15. Security: GitHub token only via env; CORS allowlist (`CORS_ORIGINS`, default none in prod, `http://localhost:5173` in dev); security headers set by Caddy; no auth on any endpoint (all public read-only).

## Output rules for area agents
- Write TWO files in the directory you are told: `facts-<area>.md` (verified facts with URLs; UNVERIFIED items listed separately) and `draft-<area>.md` (the plan section, final-quality, ready to paste into docs/PLAN.md).
- Draft length: as long as needed to be exact; no padding. Prefer tables.
- Return ONLY the structured summary requested; the files are the real output.
