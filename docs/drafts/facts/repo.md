# facts-repo.md — verified facts for the repo/toolchain/frontend-delivery section

Verified 2026-09-05. Blocked hosts this session (egress proxy): svelte.dev, tailwindcss.com, go.dev, developer.chrome.com, developer.mozilla.org, www.w3.org, docs.github.com, bundlephobia.com, data.jsdelivr.com, api.github.com, codeload.github.com. Where a primary doc host was blocked, the same content was read from the project's own GitHub source repo (raw.githubusercontent.com) or the npm registry — noted per item.

## 1. SvelteKit 2 / Svelte 5 / adapter-static

| Claim | Source |
|---|---|
| svelte latest = 5.57.0 | https://registry.npmjs.org/svelte/latest |
| @sveltejs/kit latest = 2.70.3; peer svelte `^4.0.0 \|\| ^5.0.0-next.0`, vite `^5.0.3 \|\| ^6.0.0 \|\| ^7.0.0-beta.0 \|\| ^8.0.0` | https://registry.npmjs.org/@sveltejs/kit/latest |
| @sveltejs/adapter-static latest = 3.0.10; peer `@sveltejs/kit ^2.0.0` | https://registry.npmjs.org/@sveltejs/adapter-static/latest |
| @sveltejs/adapter-node latest = 5.5.7 | https://registry.npmjs.org/@sveltejs/adapter-node/latest |
| @sveltejs/vite-plugin-svelte latest = 7.3.0; peer vite `^8.0.0-beta.7 \|\| ^8.0.0`, svelte `^5.46.4` | https://registry.npmjs.org/@sveltejs/vite-plugin-svelte/latest |
| vite latest = 8.2.2; engines node `^20.19.0 \|\| >=22.12.0` | https://registry.npmjs.org/vite/latest |
| adapter-static config: `adapter({ pages: 'build', assets: 'build', fallback: undefined, precompress: false, strict: true })`; root `+layout.js` must `export const prerender = true` | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/25-build-and-deploy/50-adapter-static.md |
| adapter-static: "If your host does not render `/a.html` upon receiving a request for `/a` then you will need to set `trailingSlash: 'always'` in your root layout to create `/a/index.html` instead." | same |
| adapter-static: "You must ensure SvelteKit's `ssr` option isn't set to `false`. Otherwise, prerendering will save an empty 'shell' page" | same |
| adapter-static `fallback`: "the name of the fallback page to be generated by SvelteKit, which is used as the entry point for URLs that have not been prerendered. This is commonly `200.html`, but can vary … avoid `index.html`"; `strict`: "checks that either all pages and endpoints … were prerendered, or you have the `fallback` option set" | same |
| GitHub Pages example uses `fallback: '404.html'` | same |
| `prerender` = `true` / `false` / `'auto'`; prerenderer crawls from non-dynamic entries following `<a>`; dynamic routes need `entries` export (may be `async`) or `config.kit.prerender.entries`; "Accessing `url.searchParams` during prerendering is forbidden … only doing so in the browser (for example in `onMount`)" | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/20-core-concepts/40-page-options.md |
| `trailingSlash`: `'never'` (default) → `/about/` redirects to `/about`, writes `about.html`; `'always'` → writes `about/index.html`; `'ignore'` | same |
| Route conflicts: pages write `foo/index.html`-style/ `.html` files; endpoints should carry extensions | same |
| Universal `load`: "run on the server during SSR when the user first visits your page. They will then run again during hydration, reusing any responses from fetch requests. All subsequent invocations … happen in the browser." Server `load` "always run on the server." | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/20-core-concepts/20-load.md (line 191) |
| load `fetch`: "During server-side rendering, the response will be captured and inlined into the rendered HTML … During hydration, the response will be read from the HTML, guaranteeing consistency and preventing an additional network request"; "It can make relative requests on the server" | same (lines 281–284) |
| Prerendered server-load data is written as a prerender dependency at `add_data_suffix(event.url.pathname)`; `DATA_SUFFIX = '/__data.json'`, `HTML_DATA_SUFFIX = '.html__data.json'` | https://raw.githubusercontent.com/sveltejs/kit/main/packages/kit/src/runtime/server/page/index.js ; https://raw.githubusercontent.com/sveltejs/kit/main/packages/kit/src/runtime/pathname.js |
| Config defaults: `appDir '_app'`, `paths.relative true`, `prerender.concurrency 1`, `crawl true`, `entries ['*']`, `handleHttpError`/`handleMissingId`/`handleUnseenRoutes` default = throw, `origin 'http://sveltekit-prerender'`, `router.type 'pathname' \| 'hash'`, `output.bundleStrategy 'split' \| 'single' \| 'inline'`, `env.publicPrefix 'PUBLIC_'`, `env.privatePrefix ''` | https://raw.githubusercontent.com/sveltejs/kit/main/packages/kit/src/core/config/options.js |
| `KitConfig.env.dir` = "The directory to search for `.env` files" (default "."); `publicPrefix` marks vars safe for client; vars matching neither prefix are discarded | https://raw.githubusercontent.com/sveltejs/kit/main/packages/kit/src/exports/public.d.ts |
| adapter-node serves `/${appPath}/immutable/` with `cache-control: public,max-age=31536000,immutable`; prerendered pages get trailing-slash normalisation via 308 | https://raw.githubusercontent.com/sveltejs/kit/main/packages/adapter-node/src/handler.js |
| adapter-node env: PORT/HOST (default 0.0.0.0:3000), ORIGIN, PROTOCOL_HEADER, HOST_HEADER, ADDRESS_HEADER, XFF_DEPTH, BODY_SIZE_LIMIT (512kb); run with `node build`; deploy with `npm ci --omit dev` | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/25-build-and-deploy/40-adapter-node.md |
| SPA mode: `export const ssr = false` in `+layout.js` + `adapter({ fallback: '200.html' })`; individual pages can re-enable `ssr` + `prerender`; host must rewrite unknown paths to the fallback | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/25-build-and-deploy/55-single-page-apps.md |
| Layout groups: "Unlike normal directories, `(app)` and `(marketing)` do not affect the URL pathname of the routes inside them"; `+page@.svelte` resets to the root layout | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/30-advanced/10-advanced-routing.md |
| SPA/fallback: "If you don't have any server-side logic (i.e. `+page.server.js`, `+layout.server.js` or `+server.js` files) you can use `adapter-static` to create your SPA"; "the fallback page will always contain absolute asset paths"; individual pages re-enable `prerender = true` | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/25-build-and-deploy/55-single-page-apps.md |
| `handle` hook runs "whether that happens while the app is running, or during prerendering"; `reroute` runs before `handle` | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/30-advanced/20-hooks.md |
| `vite build` = Vite build (prerendering happens here) then adapter; guard build-time side effects with `import { building } from '$app/environment'` | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/25-build-and-deploy/10-building-your-app.md |
| Shallow routing: `import { pushState, replaceState } from '$app/navigation'`; `page.state` is empty during SSR and on first landing | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/30-advanced/67-shallow-routing.md |
| `$app/state` exports `page`, `navigating`, `updated`; changes are "available exclusively with runes" | https://raw.githubusercontent.com/sveltejs/kit/main/packages/kit/src/runtime/app/state/index.js |
| Project skeleton: `src/lib` ($lib), `src/lib/server` ($lib/server, server-only), `src/params`, `src/routes`, `src/app.html`, `src/error.html`, `src/hooks.client.js`, `src/hooks.server.js`, `src/service-worker.js`, `src/instrumentation.server.js`, `static/`, `tests/`, `package.json`, `svelte.config.js`, `tsconfig.json`, `vite.config.js` | https://raw.githubusercontent.com/sveltejs/kit/main/documentation/docs/10-getting-started/30-project-structure.md |
| `$effect`: "only run in the browser, not during server-side rendering"; runs after mount; may return a teardown; `$effect.pre` runs before DOM updates; prefer `$derived` over effects for derived state | https://raw.githubusercontent.com/sveltejs/svelte/main/documentation/docs/02-runes/04-$effect.md |
| `bind:this`: "The value will be `undefined` until the component is mounted — … read it inside an effect or an event handler" | https://raw.githubusercontent.com/sveltejs/svelte/main/documentation/docs/03-template-syntax/12-bind.md |
| Vite `server.proxy` example (`'/api': { target, changeOrigin, rewrite }`), built on http-proxy-3; default port 5173 | https://raw.githubusercontent.com/vitejs/vite/main/docs/config/server-options.md |
| sv CLI 0.17.0: `npx sv create [options] [path]` with `--template minimal\|demo\|library`, `--types ts\|jsdoc`, `--add [add-ons...]`, `--no-add-ons`, `--install <pm>`, `--no-install`; `npx sv add [add-ons]` (`-C`, `--no-install`); official add-ons incl. eslint, playwright, prettier, tailwindcss, vitest; `npx sv add tailwindcss` installs the Tailwind Vite plugin and edits `layout.css` + `+layout.svelte` | https://registry.npmjs.org/sv/latest ; https://raw.githubusercontent.com/sveltejs/cli/main/documentation/docs/20-commands/10-sv-create.md ; …/20-sv-add.md ; …/30-add-ons/50-tailwind.md |

## 2. Tailwind v4

| Claim | Source |
|---|---|
| tailwindcss latest = 4.3.3 | https://registry.npmjs.org/tailwindcss/latest |
| @tailwindcss/vite latest = 4.3.3; peer vite `^5.2.0 \|\| ^6 \|\| ^7 \|\| ^8` | https://registry.npmjs.org/@tailwindcss/vite/latest |
| Wiring (vite plugin `tailwindcss()` + `@import "tailwindcss"` in the CSS entry) is what the `sv add tailwindcss` add-on generates ("Tailwind setup following the Tailwind for SvelteKit guide … the Tailwind Vite plugin … updates `layout.css` and `+layout.svelte`") | https://raw.githubusercontent.com/sveltejs/cli/main/documentation/docs/30-add-ons/50-tailwind.md |

## 3. Schema tooling

| Claim | Source |
|---|---|
| json-schema-to-typescript latest = 16.0.0, bin `json2ts` | https://registry.npmjs.org/json-schema-to-typescript/latest |
| json2ts actively maintained: commits dated 2026-09-01 and 2026-09-03 on master | https://github.com/bcherny/json-schema-to-typescript/commits/master |
| json2ts CLI: `json2ts -i foo.json -o foo.d.ts`, `-i 'schemas/**/*.json' -o types/`, `--unreachableDefinitions`, `--cwd`, `--additionalProperties`, `--bannerComment`, `--declareExternallyReferenced`, `--style.*`; draft-04 core model, "supports … draft 6, 7, 2019-09, and 2020-12 … a `$schema` declaration does not change the output"; enums supported (`tsEnumNames` for string enums); `oneOf` "Treated like `anyOf`"; not expressible: `format`, `pattern`, `minimum/maximum`, `uniqueItems`, `dependencies` | https://github.com/bcherny/json-schema-to-typescript |
| quicktype latest = 26.0.0; `quicktype -s schema schema.json -l ts -o out.ts --just-types` (emits runtime converters unless `--just-types`) | https://registry.npmjs.org/quicktype/latest ; https://raw.githubusercontent.com/glideapps/quicktype/master/README.md |
| invopop/jsonschema latest = v0.14.0 (2026-04-23); emits `"$schema": "https://json-schema.org/draft/2020-12/schema"`; requires Go ≥ 1.24; tags: `required, enum, oneof_required, oneof_type, anyof_required, anyof_type, default, example, minimum, maximum, minLength, maxLength, pattern, format, title, description` (+ more); enums via repeated `enum=` tags; fields required unless `json:",omitempty"`/`omitzero`, or opt into `RequiredFromJSONSchemaTags`; Reflector options `Anonymous, ExpandedStruct, DoNotReference, AllowAdditionalProperties, RequiredFromJSONSchemaTags, FieldNameTag, KeyNamer, Namer, Mapper, AddGoComments(base, path)`; custom `JSONSchema()`, `JSONSchemaExtend()`, `JSONSchemaAlias()`, `JSONSchemaProperty()`; `jsonschema_extras` tag | https://pkg.go.dev/github.com/invopop/jsonschema ; https://raw.githubusercontent.com/invopop/jsonschema/main/README.md |
| santhosh-tekuri/jsonschema/v6 latest = v6.0.3 (2026-06-28); drafts 4/6/7/2019-09/2020-12; `jsonschema.UnmarshalJSON(r)` → `c.AddResource(name, schema)` → `c.Compile(name)` → `sch.Validate(inst)`; `FileLoader`/`UseLoader` for `fs.FS` | https://pkg.go.dev/github.com/santhosh-tekuri/jsonschema/v6 |

## 4. Go toolchain and libraries

| Claim | Source |
|---|---|
| Newest Go tags: go1.27.1 and go1.26.8 (2026-09-01), go1.27.0 (2026-08-19), go1.25.14 (2026-08-19) | https://github.com/golang/go/tags |
| Docker library `golang` publishes 1.27.1 and 1.26.8 with variants trixie, bookworm, alpine3.24, alpine3.23 | https://raw.githubusercontent.com/docker-library/golang/master/versions.json |
| GOTOOLCHAIN=auto: "When the `go` or `toolchain` line is newer than the bundled toolchain, the `go` command runs the newer toolchain instead … otherwise downloads and caches a copy"; omitted `toolchain` line is implicitly `toolchain go<V>` | https://raw.githubusercontent.com/golang/website/master/_content/doc/toolchain.md |
| go.opentelemetry.io/otel latest = v1.46.0 (2026-08-25); its go.mod says `go 1.26.0`; supported Go 1.25/1.26/1.27 | https://pkg.go.dev/go.opentelemetry.io/otel ; https://raw.githubusercontent.com/open-telemetry/opentelemetry-go/main/go.mod |
| otel sdk/trace v1.46.0: `SpanExporter{ ExportSpans(ctx, []ReadOnlySpan) error; Shutdown(ctx) error }`, `NewBatchSpanProcessor(exporter, opts...)`, `NewSimpleSpanProcessor(exporter)` (testing only), `NewTracerProvider(opts...)` | https://pkg.go.dev/go.opentelemetry.io/otel/sdk/trace |
| otelhttp latest = v0.71.0 (2026-08-26): `NewHandler(handler, operation, opts...)`, `NewMiddleware(operation, opts...)`, `WithFilter(Filter)` | https://pkg.go.dev/go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp |
| prometheus/client_golang latest = v1.24.1 (2026-07-24); go.mod `go 1.25.0` | https://pkg.go.dev/github.com/prometheus/client_golang ; https://raw.githubusercontent.com/prometheus/client_golang/main/go.mod |
| go-chi/chi/v5 latest = v5.3.2 (2026-08-20); supports the four most recent Go majors | https://pkg.go.dev/github.com/go-chi/chi/v5 |
| chi middleware v5.3.2 has RequestID, Recoverer, Timeout, Compress, SetHeader, NoCache, Heartbeat, Throttle, StripSlashes, GetHead; `RealIP` is DEPRECATED ("vulnerable to IP spoofing"); replacements `ClientIPFromHeader(h)`, `ClientIPFromXFF(trustedIPPrefixes ...string)`, `ClientIPFromXFFTrustedProxies(n)`, `ClientIPFromRemoteAddr`, read via `GetClientIP(ctx)` | https://pkg.go.dev/github.com/go-chi/chi/v5/middleware |
| modernc.org/sqlite latest = v1.58.0 (2026-09-01): "a sql/database driver using a CGo-free port of the C SQLite3 library", driver name `sqlite`, SQLite 3.53.4, linux amd64/arm64 supported; go.mod `go 1.25.0` | https://pkg.go.dev/modernc.org/sqlite ; https://gitlab.com/cznic/sqlite/-/raw/master/go.mod |
| golang.org/x/time/rate latest = v0.15.0 (2026-02-11): `NewLimiter(r Limit, b int)`, `(*Limiter).Allow()` | https://pkg.go.dev/golang.org/x/time/rate |
| gopkg.in/yaml.v3 latest = v3.0.1; `yaml.Node`; `Decoder.KnownFields(true)` strict decoding | https://pkg.go.dev/gopkg.in/yaml.v3 |
| `embed`: patterns "may not contain '.' or '..' … nor begin or end with a slash"; directory embedding excludes names beginning with '.' or '_' unless the `all:` prefix is used; each pattern must match ≥1 file or non-empty dir or the build fails; `embed.FS` implements `fs.FS` → `http.FileServer(http.FS(content))` | https://pkg.go.dev/embed |
| net/http: `FileServerFS(root fs.FS) Handler`, `FS(fsys fs.FS) FileSystem`, `ServeFileFS(w, r, fsys, name)`; requests ending in `/index.html` are redirected to the directory path | https://pkg.go.dev/net/http#FileServer |
| fogleman/gg latest = v1.3.0 (2019-02-20), pure Go (deps golang.org/x/image, github.com/golang/freetype); `NewContext(w,h)`, `SetFontFace(font.Face)`, `DrawStringWrapped`, `DrawRoundedRectangle`, `EncodePNG(io.Writer)`; "font.Face objects are not thread safe" | https://pkg.go.dev/github.com/fogleman/gg ; https://pkg.go.dev/github.com/fogleman/gg#Context.SetFontFace |
| golang.org/x/image v0.45.0 (2026-08-11) `font/opentype`: `Parse(src []byte) (*Font, error)`, `NewFace(f *Font, opts *FaceOptions) (font.Face, error)`; `Font` = `sfnt.Font`, methods safe for concurrent use with separate buffers | https://pkg.go.dev/golang.org/x/image/font/opentype |
| github.com/golang/freetype/truetype: `Parse(ttf []byte) (*Font, error)`, `NewFace(f *Font, opts *Options) font.Face` (Size/DPI/Hinting); pseudo-version v0.0.0-20170609003504-e2365dfdc4a0 | https://pkg.go.dev/github.com/golang/freetype/truetype |
| promtool is a main package in module github.com/prometheus/prometheus (v0.314.0, 2026-08-17); `curl -s http://localhost:9090/metrics \| promtool check metrics --extended` | https://pkg.go.dev/github.com/prometheus/prometheus/cmd/promtool ; https://raw.githubusercontent.com/prometheus/prometheus/main/docs/command-line/promtool.md |
| golangci-lint latest = v2.13.2 (2026-08-27) | https://github.com/golangci/golangci-lint/releases |

## 5. Containers and CI

| Claim | Source |
|---|---|
| Distroless lists `gcr.io/distroless/static-debian13`, `static-debian12`, `base-debian13`, `base-debian12`, `base-nossl-debian12`, `cc-debian12`; tags `latest, nonroot, debug, debug-nonroot`; Go example: `RUN CGO_ENABLED=0 go build …` then `FROM gcr.io/distroless/static-debian13`; "Statically compiled applications (Go) that do not require libc can use the `gcr.io/distroless/static` image"; static contains ca-certificates, /etc/passwd, /tmp, tzdata | https://raw.githubusercontent.com/GoogleContainerTools/distroless/main/README.md ; https://github.com/GoogleContainerTools/distroless/blob/main/base/README.md |
| Docker library `node`: 22 (Jod) and 24 (Krypton) with bookworm, bookworm-slim, trixie, alpine3.23/3.24 variants | https://raw.githubusercontent.com/nodejs/docker-node/main/versions.json |
| Node 22: Maintenance LTS, EOL 2027-04-30; Node 24: Active LTS (maintenance from 2026-10-20), EOL 2028-04-30; Node 26 Current | https://raw.githubusercontent.com/nodejs/Release/main/README.md |
| actions/checkout@v7 (fetch-depth default 1) | https://github.com/actions/checkout |
| actions/setup-go@v7: `go-version-file` (go.mod/go.work/.go-version/.tool-versions), `cache` default true, `cache-dependency-path` | https://github.com/actions/setup-go |
| actions/setup-node@v7: `node-version-file` (.nvmrc/.node-version/package.json…), `cache: npm\|yarn\|pnpm`, `cache-dependency-path` | https://github.com/actions/setup-node |
| docker/build-push-action@v7 with docker/setup-buildx-action@v4, docker/login-action@v4, (optional setup-qemu-action@v4); inputs context, file, push, tags, platforms, cache-from/cache-to (`type=gha`), build-args, target; use Path context with checkout so prior step mutations are seen | https://github.com/docker/build-push-action |
| docker/metadata-action@v6: `tags: type=semver,pattern={{version}}` / `type=sha`; outputs `tags`, `labels`, `version` | https://github.com/docker/metadata-action |
| GHCR login: `docker/login-action@v4` with `registry: ghcr.io`, `username: ${{ github.actor }}`, `password: ${{ secrets.GITHUB_TOKEN }}` | https://github.com/docker/login-action |
| GitHub docs GHCR example: `permissions: packages: write, contents: read`; wires `tags: ${{ steps.meta.outputs.tags }}`, `labels: ${{ steps.meta.outputs.labels }}` | https://raw.githubusercontent.com/github/docs/main/content/actions/tutorials/publish-packages/publish-docker-images.md |
| Workflow syntax: tag filters `v*`, `v1.**` ("`**` matches any character including slash"); `concurrency: { group, cancel-in-progress }`; `permissions: contents: read / packages: write` | https://raw.githubusercontent.com/github/docs/main/content/actions/reference/workflows-and-actions/workflow-syntax.md |
| Caddy `reverse_proxy localhost:9005`; sets `X-Forwarded-For`, `X-Forwarded-Proto`, `X-Forwarded-Host`; global `servers > trusted_proxies` recommended | https://raw.githubusercontent.com/caddyserver/website/master/src/docs/markdown/caddyfile/directives/reverse_proxy.md |
| Caddy `encode`: "If omitted, `zstd` (preferred) and `gzip` are enabled by default"; `encode zstd gzip` | https://raw.githubusercontent.com/caddyserver/website/master/src/docs/markdown/caddyfile/directives/encode.md |

## 6. Frontend libraries

| Claim | Source |
|---|---|
| uPlot: "a small (~50 KB min), fast chart for time series, lines, areas, ohlc & bars"; features: multiple series with toggles, multiple y-axes/scales, cursor/crosshair, legend with live values, sync cursors; non-features: "No stacked series" (by design), no animations, no built-in data processing; used by Grafana; wrappers exist for React/Vue/Svelte | https://raw.githubusercontent.com/leeoniya/uPlot/master/README.md ; https://github.com/leeoniya/uPlot |
| uplot npm latest = 1.6.32; `main ./dist/uPlot.cjs.js`, `module ./dist/uPlot.esm.js`, `types ./dist/uPlot.d.ts` | https://registry.npmjs.org/uplot/latest |
| Measured from the npm tarball (`npm pack uplot@1.6.32`, gzip -9): `dist/uPlot.iife.min.js` 51.1 kB → 22.0 kB gzip; `dist/uPlot.min.css` 1.9 kB → 0.8 kB gzip | local measurement |
| uPlot ships stacking as a demo, not core: `demos/stacked-series.html`, `demos/stack.js`, plus `bars-grouped-stacked.html`, `resize.html`, `sync-cursor.html`, `cursor-tooltip.html`, `zoom-touch.html`, `timeline-discrete.html` | https://github.com/leeoniya/uPlot/tree/master/demos |
| gridstack npm latest = 13.2.0; "TypeScript/JS lib for dashboard layout and creation, responsive, mobile support, no external dependencies"; `types ./dist/gridstack.d.ts`; dependencies: none | https://registry.npmjs.org/gridstack/latest |
| gridstack: native touch since v6; `columnOpts` breakpoints (v10+); `grid.save()`/`grid.load()`; `change` event; Angular/React/Vue wrappers (no Svelte wrapper) | https://github.com/gridstack/gridstack.js |
| Measured (`npm pack gridstack@13.2.0`, gzip -9): `dist/gridstack-all.js` 88.1 kB (minified UMD) → 23.8 kB gzip; `dist/gridstack.min.css` 5.5 kB → 1.3 kB gzip | local measurement |
| typescript latest tag = 7.0.2; stable 6.0.3 and 5.9.3 exist | https://registry.npmjs.org/typescript/latest ; `npm view typescript versions` |
| svelte-check 4.7.6; peer typescript `^5.0.0 \|\| ^6.0.0` | https://registry.npmjs.org/svelte-check/latest ; `npm view svelte-check@4.7.6 peerDependencies` |
| typescript-eslint 8.69.0; peer typescript `>=4.8.4 <6.1.0`, eslint `^8.57.0 \|\| ^9.0.0 \|\| ^10.0.0` | `npm view typescript-eslint@8.69.0 peerDependencies` |
| eslint 10.10.0; eslint-plugin-svelte 3.23.0 (peer eslint `^8.57.1 \|\| ^9 \|\| ^10`); prettier 3.9.6; prettier-plugin-svelte 4.1.1 (peer svelte ^5, prettier ^3); vitest 5.0.0; @playwright/test 1.63.0; @axe-core/playwright 4.13.0; @lhci/cli 0.15.1 | https://registry.npmjs.org/<pkg>/latest for each |

## 7. Fonts, Lighthouse, accessibility

| Claim | Source |
|---|---|
| Inter: SIL OFL 1.1; latest release v4.1 (asset `Inter-4.1.zip`, 32.1 MB) | https://github.com/rsms/inter ; https://github.com/rsms/inter/releases/expanded_assets/v4.1 |
| JetBrains Mono: OFL-1.1, free for commercial use; latest release v2.304 (asset `JetBrainsMono-2.304.zip`); variable woff2 files are generated into the fonts folder | https://github.com/JetBrains/JetBrainsMono ; https://github.com/JetBrains/JetBrainsMono/releases/expanded_assets/v2.304 |
| `<link rel="preload" as="font" type="font/woff2" crossorigin>`: fonts "have to be fetched using anonymous-mode CORS", so `crossorigin` is required "even when the fetch is not cross-origin" | https://raw.githubusercontent.com/mdn/content/main/files/en-us/web/html/reference/attributes/rel/preload/index.md |
| `font-display`: `swap` = "extremely small block period and an infinite swap period"; `optional` = "extremely small block period and no swap period"; `fallback`; `block`; `auto` | https://raw.githubusercontent.com/mdn/content/main/files/en-us/web/css/reference/at-rules/@font-face/font-display/index.md |
| `@media (prefers-reduced-motion: reduce)` — values `no-preference` / `reduce` | https://raw.githubusercontent.com/mdn/content/main/files/en-us/web/css/reference/at-rules/@media/prefers-reduced-motion/index.md ; https://raw.githubusercontent.com/w3c/csswg-drafts/main/mediaqueries-5/Overview.bs |
| WCAG 2.2 SC 2.5.8 Target Size (Minimum): targets ≥ 24×24 CSS px, exceptions spacing/equivalent/inline/user-agent/essential | https://raw.githubusercontent.com/w3c/wcag/main/understanding/22/target-size-minimum.html |
| Lighthouse performance weights: FCP 10, LCP 25, TBT 30, CLS 25, Speed Index 10; accessibility category includes `target-size` (7), `meta-viewport` (10), `color-contrast` (7), `html-has-lang`, `image-alt`, `link-name`, `button-name`, `heading-order`; SEO includes `document-title`, `meta-description`, `robots-txt`, `is-crawlable`, `link-text`; best-practices includes `csp-xss`, `errors-in-console`, `deprecations` | https://raw.githubusercontent.com/GoogleChrome/lighthouse/main/core/config/default-config.js |
| Lighthouse 13.4.1 (2026-07-20); 13.0.0 removed `uses-rel-preload`/`preload-fonts` and `font-size` audits; 12.5.0 added the "font display insight"; accessibility score is a weighted average of pass/fail audits (no partial credit) | https://raw.githubusercontent.com/GoogleChrome/lighthouse/main/changelog.md ; https://raw.githubusercontent.com/GoogleChrome/lighthouse/main/docs/scoring.md |

## UNVERIFIED

- The exact file names of the variable web fonts inside `Inter-4.1.zip` and `JetBrainsMono-2.304.zip` (only the zip names were visible). Plan refers to them generically as `InterVariable.woff2` / `JetBrainsMono[wght].woff2`; confirm at Phase 3 when the zips are downloaded.
- golangci-lint install method for CI (official install script / GitHub action major version) — release page reachable, install docs not fetched. Plan pins the version (v2.13.2) and leaves the install command to Phase 1 to copy from the release notes.
- Svelte 5 `.svelte.ts` module files for shared rune state — not re-verified this session (core Svelte 5 feature).
- `$env/static/private` reference page (svelte.dev blocked; generated doc not found in the repo) — the `KitConfig.env` doc comment (verified above) confirms the prefix semantics and that private vars are "unsafe to expose to client-side code".
- Exact `node:24` patch version in the Docker library (the versions.json summary did not expose it); plan pins the `24-bookworm-slim` major tag.
- `font-display` Lighthouse insight UIStrings (defined in an external trace_engine module) — the presence of the insight is verified via the changelog.
- Caddy automatic HTTPS / Let's Encrypt behaviour and `header` directive — not fetched (Phase 5 will verify; only named at one-paragraph depth here).
