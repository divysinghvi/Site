# Build round 1 hand-off notes (from the foundation, content, PromQL and web-core builders)

## Wrote the complete Phase 2 content/ tree: spans.yaml (43 spans, 10 services, every brief §3.1 span incl. 15 mi…

**Summary.** Wrote the complete Phase 2 content/ tree: spans.yaml (43 spans, 10 services, every brief §3.1 span incl. 15 minikube PR stubs, 2 kubeflow stubs, 4 incident spans with "outage resolved" events, month-precision YYYY-MM dates, palette colors, postmortem links), logs.ndjson (90 lines, every span and every service referenced; levels info 66 / debug 16 / warn 4 / error 4; 15 distinct components), postmortems INC-001..004 (84–96 lines each, fixed 8-H2 order, severity with TODO(divy) confirm comment, TODO(divy) timelines, sanitized generic hostnames), panels.yaml (12 panels on the 24-col grid, all exprs parse under promtool and the subset; review fixes applied: pypi rate[2d]*86400, github_stars > 0, hide semantics), alerts.yaml (3 required alerts, promtool check rules SUCCESS, runbook_url /dashboard#panel=<id>), uptime.yaml (5 targets, self = $SITE_ORIGIN/readyz with the coverage-16 note), manual_metrics.yaml, profile.yaml (tagline TODO, 6 pods incl. euro-iam/sales-wms/codemind-ci), README.md. Created docs/DEVIATIONS.md with 3 content lines. 466 TODO(divy) markers total.

### known_gaps

- Judgement call: incident-start log lines use level `error` (content.md §C.4.1 reserves it for exactly that) while the brief/task list only info/warn/debug; impact lines are `warn`, resolution `info`. Recorded in docs/DEVIATIONS.md; flip the 4 `error` lines to `warn` if you want only three levels.
- Judgement call: oss.kubeflow got 2 stub children (brief says only 'contributions', plural); oss.minikube got exactly 15 stubs (brief says 15+). Divy adds/removes stubs.
- Judgement call: pods euro-iam and sales-wms are `0/1 Completed` (both engagements ended); sales-wms note carries a TODO(divy) to flip to Running if still in production.
- Judgement call: project.savely `lang` is a TODO(divy) string ('python + javascript/typescript (Svelte side)') rather than asserting JS vs TS; oss.minikube parent and PR stubs use `lang: TODO(divy)` (a PR may be docs/CI).
- Postmortem prose: mechanisms beyond the brief (e.g. 'no cgroup memory limits', 'apps read .env once at boot', 'Sentry drops silently in the SDK') are stated as contributing-factor reasoning consistent with the brief's one-line descriptions, not as confirmed facts; every specific (dates, durations, tools, config keys, detection path, port-2465 rationale) is TODO(divy). Divy should review these at the checkpoint.
- The root span carries a `stack` tag copied from the brief's 'Stack I actually use' list and an `interests` tag; the euro-iam.* child spans carry stack tags derived from the parent's stated stack (gin/gorm/postgres/redis/asynq/webauthn/totp).
- edu.btech-ece is `open: true` with a planned `end: "2027"` (resolves to 2028-01-01, after the root's now-end): the Go validator's child⊆parent rule must exempt planned ends of open spans, as the draft's own tree assumes.
- Deep-link forms used: alert runbook_url `/dashboard#panel=<id>` (coverage-09); log lines link postmortems as `/postmortems/INC-00N` in a free-form `postmortem` field — the frontend route must exist at that path (repo draft §R.7.1 has `/postmortems/[id]`).

### notes_for_next

- Content root: /home/user/Site/content/ — files: spans.yaml, logs.ndjson, panels.yaml, alerts.yaml, uptime.yaml, manual_metrics.yaml, profile.yaml, README.md, postmortems/INC-001.md..INC-004.md. Schemas per docs/drafts/content.md with the review-findings fixes applied.
- uptime.yaml self-api url is the literal string `$SITE_ORIGIN/readyz` (NOT a URI). The content loader must expand `$SITE_ORIGIN` from env before `format: uri` validation; if SITE_ORIGIN is unset treat the target as unconfigured (never green). The target also has a `note` field (new, per coverage-16) that /api/content/uptime must pass through and the uptime page must render.
- profile.yaml has a new top-level `tagline` field (coverage-13) — add it to the Go Profile struct/schema and serve via /api/content/profile; used for <title>/og default. profile.pods has 6 rows; euro-iam and sales-wms use status `Completed` with ready `0/1`.
- panels.yaml: `hide: true` means 'run the query, do not draw it, expose latest value for the last-updated stamp' (coverage-14) — opposite of Grafana. pypi-downloads expr is `rate(pypi_downloads_total{package="codemind-ci"}[2d]) * 86400` (promql P1); stars-by-repo is `github_stars > 0`. divy_experience_years must be a live series computed from spans.yaml (earliest non-TODO start among counts_as_experience services → 2024-05 today).
- alerts.yaml is a pure Prometheus rule file (promtool check rules passes). runbook_url values: `/contact`, `/dashboard#panel=commits-weekly`, `/dashboard#panel=lfx-pending` — hash.ts needs a `panel=<id>` key (scroll + flash). The HighContributionRate threshold TODO lives in a YAML comment (the todo inventory must scan comments per §C.2.3).
- logs.ndjson: 90 lines; free-form fields are flat scalars only. Numeric free-form fields present: months_with_team, containers_approx, port, active_users, global_rank, stage; booleans: resolved. Component label has 15 values. 4 lines use level `error`. Lines with `"ts":"TODO(divy)"` rely on the span-start fallback (§C.4.1); 8 lines have no span (community x2, oss playwright x1, divy x3 have span divy.career — only community x2 and playwright x1 lack `span`).
- spans.yaml: 43 spans. Only gradr.inc-00N spans carry `{kind: postmortem, ref: INC-00N}` links (bidirectional rule); gradr.observability deliberately has no postmortem links. Every incident span has an `outage resolved` event with TODO ts. Tag values include lists (stack/lang/interests/open_to) and one TODO-with-note string on project.savely `lang` — the TagValue union must accept scalars and string lists. Some event attrs are strings like "TODO(divy)" (attrs must accept scalars only).
- Postmortem frontmatter has a YAML comment after `severity:` (`# TODO(divy): confirm severity …`) — the frontmatter parser must tolerate comments (yaml.v3 does) and the todo inventory should report them. Titles ≤79 chars, summaries ≤159 chars, all durations/dates TODO(divy), status resolved, services [gradr].
- docs/DEVIATIONS.md exists (created by W0) with 3 content lines; append-only, one line per deviation.
- Self-check script (reusable): /tmp/claude-0/-home-user-Site/66ca9d85-1945-5eac-9650-eaf9e3145ec4/scratchpad/check.py — Python implementation of the §C.10.1 rules (ids, dates, links, logs, postmortem sections, grid overlap, manual-source, alerts, sanitizer). Useful as a reference when writing `divy validate` tests; run from /home/user/Site.
- The orchestrator's checkpoint commits already track content/*.yaml, logs.ndjson and postmortems; content/README.md and docs/DEVIATIONS.md are untracked as of this summary.

## Built the Go API foundation in the Vercel layout: root module `divy.dev` (go 1.24.0, Go-1.24-compatible pins: …

**Summary.** Built the Go API foundation in the Vercel layout: root module `divy.dev` (go 1.24.0, Go-1.24-compatible pins: chi v5.3.2, modernc.org/sqlite v1.46.1, libsql-client-go, yaml.v3, invopop/jsonschema v0.14, santhosh-tekuri v6, goldmark, bluemonday, client_golang v1.23.2), `cmd/api/main.go` = the `divy` CLI (serve default, collect, validate, schemagen, migrate, export-ascii, ping, version; PORT wins over --addr/DIVY_ADDR; no subcommand ⇒ serve from env), `internal/model` (every content type incl. custom Scalar/TagValue/IntOrList/DateOrTodo/LogLine-with-Extra, every API envelope, APIRoots + SchemaRoots), `internal/schemagen` (draft 2020-12 files + index, in-memory compile for validation; `schema/` generated and drift-checked), `internal/content` (loader, JSON-Schema pass, all C.10.1 rules incl. sanitizer, TODO inventory with line/col/path/context for YAML values+comments/NDJSON/markdown, span tree with TODO fallbacks + DFS order, Jaeger JSON with the fixed ids, goldmark+bluemonday postmortem HTML with fixed slugs + TOC, ordered logs), `internal/ascii` (80-col waterfall per L.6.2 with golden test), `internal/store` (file: modernc WAL + single-writer goroutine / libsql:// direct writes, TURSO_* override, embedded idempotent migrations incl. collector_state, full read/write/retention API), `internal/collector` (Collector/Registry/Runner/Scheduler with jitter + single-flight, RunRound with budget + per-collector timeout + collector_runs rows + /api/collect summary, no-op "process" collector), `internal/server` (chi router, recover/security-headers/request-context/logging-without-IPs/timeout/GetHead, Hooks.Outer/Inner/Mount slots for OTel+rate-limit+cache+CORS and later route families, /healthz exact JSON, /readyz with 503 semantics, /robots.txt, `/` Accept negotiation + /ascii, /api/content/*, /api/traces/{id} (career alias + otel rows→Jaeger), /api/traces?service=, /api/services*, /api/operations, GET|POST /api/collect with bearer auth 401/200, embedded-dist static handler with 200.html/404.html fallbacks and Vercel Cache-Control values, family-specific 404/405 envelopes), Makefile (marked web section), .env.example, .gitignore, .golangci.yml, docs/DEVIATIONS.md, deploy/README.md. gofmt/vet/golangci-lint clean, `go test -race ./...` green (store, collector round/budget/timeout, content rules table with 38 generated fixtures, Jaeger, ASCII golden, httptest for every route), live curl checks against :18080 and PORT=18080-with-no-subcommand pass. `content/` (written concurrently) validates with 3 content-side errors in spans.yaml (flow-mapping commas / missing colon), 0 warnings, 266 TODO(divy).

### known_gaps

- No OTel: the server never emits X-Divy-Trace-Id/X-Divy-Trace-Sampled (deliberately, no fake ids); /api/traces/{id} for OTel ids reads store.ReadTrace directly with no ForceFlush-and-retry; the `internal/trace` package, exporter and traceHeader middleware go into server.Hooks.Outer.
- No rate limiting, response cache, ETag/304 on API responses, CORS, X-Cache; slots are server.Hooks.Inner (order clientIP → rateLimit → cors → cache). Static files already get strong ETags + 304.
- Not mounted yet: /metrics, /api/v1/*, /loki/*, /favicon.svg, /og/*, /api/uptime(/heartbeats); they must be registered through server.Hooks.Mount (the /api/v1, /loki and /api/* family 404 catch-alls are registered after Mount so real routes win). model.UptimeHeartbeats & friends already exist for the heartbeats endpoint.
- Rules panels.expr and alerts expression parsing only reject empty expressions until the PromQL parser is plugged into content.Options.CheckExpr; alerts.rulefmt is structural (no prometheus rulefmt dependency).
- libsql/Turso remote mode is implemented (sql.Open("libsql", url) with ?authToken=) but only unit-tested for URL classification; no live Turso run was possible in the sandbox.
- Real collectors (github, pypi, uptime, manual, retention) are not implemented; only the no-op `process` collector is registered, so /readyz shows just `process` and /api/collect reports one collector.
- go.mod says `go 1.24.0` (not the literal `go 1.24`): `go mod tidy` rewrites it because modernc.org/sqlite v1.46.1 declares go 1.24.0; recorded in docs/DEVIATIONS.md.
- The ASCII draft example assumed t1 = 2027-01-01 for planned end "2027"; the implementation follows the end-resolution rule (YYYY as an end = next Jan 1 → 2028-01-01), so the golden shows 1 col ≈ 1.6 mo over 38 bar columns.
- No web/ yet: `/` with a browser Accept returns 404 {"error":"web assets not embedded; …"}; Makefile web targets assume web scripts dev|build|check|gen:types and adapter-static output into internal/web/dist.

### notes_for_next

- Layout: module `divy.dev` at repo root; binary `go build ./cmd/api` (Makefile `make build` → bin/divy with -ldflags -X divy.dev/internal/version.{Version,Commit,Date,Branch,BuildUser}). Packages: internal/{model,schemagen,content,ascii,store,collector,server,web,config,version}. Run everything from the repo root (schemagen reads Go comments from ./internal/model relative to cwd).
- CLI (cmd/api/main.go): `divy` with no subcommand = `serve` from env. serve flags: --addr (PORT env wins, then DIVY_ADDR, default :8080), --db (DIVY_DB_URL default file:./data/divy.db; TURSO_DATABASE_URL + TURSO_AUTH_TOKEN win), --content (DIVY_CONTENT_DIR default ./content), --site-origin (SITE_ORIGIN), --collect (in-process scheduler), --log-level/--log-format (DIVY_LOG_LEVEL/DIVY_LOG_FORMAT text|json). Other subcommands: collect --once [--only a,b] [--budget], validate [--content] [--strict] [--list-todos] [--json] [dir], schemagen --out ./schema [--check] [--comments internal/model], migrate [--db] [--to N] (prints status table), export-ascii [--content] [--width] [--color] [--origin], ping --url --timeout, version. Startup fails (exit 1, report on stderr) on content errors.
- Env parsing lives in internal/config.FromEnv(getenv) → config.Config (Addr, DBURL, ContentDir, SiteOrigin, CollectTokens = DIVY_COLLECT_TOKEN + CRON_SECRET, CollectBudget = DIVY_COLLECT_BUDGET default 8s, GitHubToken = DIVY_GITHUB_TOKEN only (never GITHUB_TOKEN/GH_TOKEN), GitHubLogin, GitHubPrivateOrgs, PyPIPackages, UptimeSelfURL (default SITE_ORIGIN/readyz), ProbeTimeout, Interval{GitHub,PyPI,Uptime,Manual,Retention}, CollectDisabled, CORSOrigins, TrustedProxies, QueryLookback = QUERY_LOOKBACK_DELTA default 26h, OTelServiceName). config.ParseDuration accepts Go durations plus d/w/y. Add new knobs there and in .env.example.
- internal/model: content structs SpansFile/Service/Span/Event/Link, LogLine (fixed fields + Extra map[string]any via custom (Un)MarshalJSON; JSONSchemaExtend adds additionalProperties scalar + propertyNames), PostmortemFrontmatter, PanelsFile/Panel/Target/GridPos/Thresholds/PanelSource, AlertsFile/AlertGroup/AlertRule, UptimeFile/UptimeTarget (ExpectedStatus IntOrList, Note), ManualMetricsFile/ManualMetric, Profile (has Tagline per coverage-13)/ProfileLinks/Escalation/Pod. Custom types: DateOrTodo, Scalar{Value}, TagValue{Value string|int64|float64|bool|[]string}, IntOrList; enums are named string types with JSONSchema() (each becomes its own $defs entry / TS union). API types: Jaeger* (JaegerTraceResponse/JaegerTrace/JaegerSpan/JaegerKeyValue/JaegerLog/JaegerProcess/JaegerReference/JaegerStringsResponse/JaegerOperationsResponse), Healthz, Readyz(+ReadyzChecks/DB/Content/Collector with nullable pointers), ContentServices/ContentPostmortemList/PostmortemSummary/ContentPostmortem/TOCEntry/ContentUptime/UptimeTargetView/ContentManualMetrics/ContentProfile/PodView/ContentTodos/TodoItem, UptimeHeartbeats (+HeartbeatTarget/ProbeLast/UptimeWindows/HeartbeatBucket/UptimeIncident), CollectSummary/CollectorResult, PlainError, PromError. model.APIRoots is the struct whose fields are every API envelope → add Prom*/Loki* result types there for schema/api.schema.json; model.SchemaRoots lists the files. Use `jsonschema:"nullable"` for `T | null`.
- internal/schemagen: Generate(Options{CommentsDir}) → []Document (per root + index); Write(dir, docs); Check(dir, docs) → drifted names; Compile(docs) → Compiled map[root]*santhosh.Schema; MustCompile(). $id base https://divy.dev/schema/<name>.schema.json. `go run ./cmd/api schemagen --out ./schema --check` is the CI drift guard (make gen-check). Regenerate schema/ after any model change.
- internal/content: Load(dir, Options{Now, SelfURL, SiteOrigin, Schemas, CheckExpr}) → *Content (error only for I/O); c.Report (Errors/Warnings []Finding{File,Line,Col,Level,Rule,Path,Message}, Todos, Files) with HasErrors(strict), Write(w, strict) human format, JSON(strict), WriteTodos; MustLoad panics on errors (tests). Content fields: Spans, Logs []LogEntry{Line, Raw, Index, FileLn, TSNano, Labels}, LogsRaw, Postmortems []*Postmortem{File, Front, Markdown, Body, HTML, TOC, Sections, TodoCount}, Panels, Alerts, Uptime, Manual, Profile, Todos, Hash (sha256 of all files), Files, LoadedAt. Methods: Nodes() DFS order, Node(id), Root(), Service(id), ServiceIDs(), ServicesWithSpans(), Postmortem(id), PostmortemsFor(spanID), ExperienceStart()/ExperienceYears(t), JaegerTrace(now), Healthz(), ServicesView(), PostmortemList(origin), PostmortemView(id, origin), UptimeView()/TargetView(t), ManualView(), ProfileView(now), TodosView(generatedAt), LogsByService(). Node{Span, Parent, Depth, Children, Path, Start, StartPrecision, End, EndPrecision} with Open(), EffectiveEnd(now), AxisEnd(now), TodoDerived(), Title(). Constants: content.TraceID (9f3a0703b53d5b0aae2fb3bdacea0ff6), content.SpanHexID(id), RootSpanID, RequiredSections, ManualMetricNames, RequiredAlerts, ParseDate/EndOf/ParsePromDuration/Slug/StreamKey/CanonicalLabels/JaegerResponse(traces...). Log ordering ns = ts (or span start / root start for TODO) + line index. Uptime urls may contain the literal $SITE_ORIGIN (expanded from Options.SiteOrigin, default http://localhost:8080) and self-api is overridden by Options.SelfURL (UPTIME_SELF_URL).
- Validation rules implemented (rule ids): file.missing, yaml.parse, yaml.date-quoted, schema, spans.root, spans.id-unique, spans.service, spans.dates, spans.todo-prefix, spans.link-url, links.postmortem-bidirectional, logs.ndjson, logs.ts, logs.span, logs.cardinality (warn), logs.count (warn: 60..100), logs.coverage (warn), pm.frontmatter, pm.sections, pm.sanitize (secrets/hostnames/IPs/emails error; phone/host-name/env-value warn; example.* emails and TODO exempt), panels.grid, panels.expr (empty only until CheckExpr is wired), panels.manual-source, alerts.rulefmt (structural), alerts.threshold-matches, alerts.required, uptime.ids, manual.catalogue, profile.healthz. Test fixture set: internal/content/testdata/valid (28 spans, 11 logs, 4 INC skeletons); TestRules derives failing sets from it in temp dirs (table `ruleFixtures` in content_test.go) — add a row per new rule.
- internal/ascii: Render(c, Options{Width, Now, Origin, Color}) and ClampWidth (60..200, default 80); golden at internal/ascii/testdata/ascii.golden (regenerate with UPDATE_GOLDEN=1 go test ./internal/ascii/ after content-fixture changes).
- internal/store: Open(ctx, url) opens + WAL-checks (file mode) + runs migrations; ResolveURL(getenv) applies TURSO_DATABASE_URL/TOKEN > DIVY_DB_URL > file:./data/divy.db; ModeOf(url). Store fields URL/Mode/Path; Write(ctx, func(*sql.Tx) error) (queued to the single writer goroutine in file mode, direct in remote mode), Reader() *sql.DB, Generation() (bumped only by series/samples/probe_results writes), Ping, Checkpoint, Close (drains + wal_checkpoint TRUNCATE). Data API: Labels/CanonicalLabels/ParseLabels/ValidateSeries, EnsureSeries, UpsertSamples (finite values only), WriteSeries, DeleteOffGrid, DeleteSamples, ListSeries(matchers), QueryRange(matchers, startMs, endMs) → []SeriesData with startMs < ts <= endMs ascending (use Matcher{Name:"__name__",Type:MatchEqual,Value:metric} for the metric; NewMatcher anchors regexes), LatestPerSeries, DeleteSeriesWhere, MetricNames, WriteProbeResults/ReadProbes(target, sinceMs)/LastProbe, WriteSpans (INSERT OR IGNORE, no gen bump)/ReadTrace/SearchTraces(service, name, startNano, endNano, limit)/Operations/OperationKinds, StartRun/FinishRun/DeleteRun/RecordCollectorRun/LastSuccess/RecentRuns, GetState/SetState/DeleteState (collector_state cursors), retention: DeleteSamplesBefore(cutoffMs, prefix ""|"probe_"|"!probe_"), DeleteProbesBefore, DeleteSpansBefore, CapSpans, DeleteRunsBefore, MarkAbandonedRuns, DeleteOrphanSeries; constants RetainSamples/RetainProbes/RetainSpans/MaxSpans/RetainRuns/AbandonRunAfter. Migrations: internal/store/migrations/NNNN_name.sql (embedded, forward-only, statements split on `;`, `--` comments stripped — never put `;` in comments); schema_migrations is created by the migrator; MigrateTo(ctx, n)/Status(ctx).
- internal/collector: Collector interface {Name(); Interval(); Run(ctx) (Result{Items, Note}, error)}; return collector.ErrDisabled (or implement Disabled() bool) for an unconfigured collector → outcome "skipped", no collector_runs row. Registry (NewRegistry/Register/Collectors/Get/Names), Runner{Store, Registry, Logger, OnResult(name, outcome, dur) hook for divy_collector_* metrics}: RunOne(ctx, c, timeout) records StartRun/FinishRun and single-flights per name; RunRound(ctx, budget, names...) runs concurrently with per-collector min(RunTimeout(interval)=min(interval/2,2m), budget) and returns model.CollectSummary (Truncated when the budget deadline hit); Readiness(ctx, now) builds the /readyz collectors map (StaleAfter = max(3×interval, 15m)). Scheduler{Runner, InitialDelay, Rand}.Start(ctx)/Stop(ctx): jitter i×5s+rand(0,10s), retention waits 60s, interval×U(0.9,1.1). Register real collectors in cmd/api/main.go next to `reg.Register(collector.Process{})` (both serve and collect paths).
- internal/server: New(Config{Content, Store, Runner, Logger, Version, Commit, StartedAt, SiteOrigin, CollectTokens, CollectBudget, OTelServiceName, RequestTimeout, ShuttingDown, Now, Hooks}) → *Server (http.Handler). Hooks.Outer = middlewares right after recover (OTel + X-Divy-Trace-Id writer), Hooks.Inner = after logging/GetHead before routes (clientIP → rateLimit → cors → cache), Hooks.Mount = []func(chi.Router) to add /metrics, /api/v1/*, /loki/*, /favicon.svg, /og/*, /api/uptime* before the family 404 catch-alls. Helpers: writeJSON(w, status, cacheControl, v), writeError(w, status, msg) → {"error":…} no-store, writePromError, writeText, RequestID(ctx); Cache-Control constants CacheQ15/CacheC60/CacheNS/CacheHTML/CacheA3600/CacheIMM (API classes carry explicit s-maxage). OTelTrace(rows []store.Span) converts otel_spans rows to Jaeger (L.4.3 typing) — reuse from the trace package. Request log line: method, route pattern, status, bytes, dur_ms, req id (never client IP/UA). 405 handler computes Allow by probing the router. /api/collect accepts ?budget= only to narrow the configured budget. Static handler (static.go) serves internal/web/dist: `/`→index.html, exact files, <route>.html or <route>/index.html, trailing slash → 308, else 200.html (200) or 404.html (404); ETag + If-None-Match → 304; immutable caching for _app/immutable/* and fonts/*.
- internal/web: `//go:embed all:dist` (only dist/.gitkeep committed; .gitignore excludes the rest); web.FS(), web.HasIndex(). SvelteKit adapter-static must write into ../internal/web/dist (index.html, 200.html fallback, 404.html).
- Makefile: targets help/setup/dev/api/web-dev/web-build/web-check/web-gen (web section marked `## ---- web targets ----`, edit freely)/gen/gen-check/test/lint/validate/todos/promtool-check/build/vercel-build/docker/ascii/migrate/collect-once/clean; CGO_ENABLED=0 exported globally, `make test` re-enables cgo for -race when gcc exists; loads .env via `set -a; source .env`; check servers use 127.0.0.1:18081. .golangci.yml (v2) excludes fmt.Fprint*/Close from errcheck.
- Gotchas: santhosh-tekuri BasicOutput hides the leaf message for single-error documents — content/yaml.go uses DetailedOutput; yaml.v3 keeps unquoted timestamps as strings but unquoted YYYY becomes an int → rule yaml.date-quoted; `go mod tidy` will not keep a literal `go 1.24` (deps require 1.24.0); GOTOOLCHAIN=local is what the sandbox needs to avoid downloading go1.25 (newer modernc/client_golang releases require it); the sandbox blocks api.github.com user endpoints, pypistats.org and vercel.com — test collectors against httptest fakes.
- Content lane: content/spans.yaml has 3 YAML quoting bugs (line 200 `alert hygiene` needs a `key: value` form inside the flow mapping; line 418 quote `name: "5,000+ active users"`); once fixed `make validate` (strict) also needs 0 warnings — the current content/ has 90 log lines and 0 warnings otherwise. `divy validate --list-todos` prints the inventory; /api/content/todos serves it.

## W1 done: web/ is a SvelteKit 2 + Svelte 5 (runes) + TypeScript + Tailwind v4 app built by adapter-static into …

**Summary.** W1 done: web/ is a SvelteKit 2 + Svelte 5 (runes) + TypeScript + Tailwind v4 app built by adapter-static into ../internal/web/dist (index.html, <route>.html, __data.json, 200.html fallback, 404.html; .gitkeep restored post-build). Type generation (schema/index.schema.json → src/lib/api/types.gen.ts, committed, byte-stable), a typed API client with ApiError and the only runtime narrowing helpers, layout shell (nav for the 7 routes, theme toggle dark/light/grafana2017 tokens, skip link, focus-visible, reduced-motion gating), the hero career trace at / (Jaeger-style waterfall: one row per span in DFS order, collapsible subtrees, service colors from process tags, hatched TODO-date bars, open spans solid-to-now + dashed planned tail, event diamonds, duration labels, draggable/resizable minimap brush + zoom buttons + keyboard range sliders, right-hand drawer with tags/events/links/postmortems/TODOs/process, j/k/↑/↓/Enter/Esc/←/→ keyboard nav on an ARIA tree with roving tabindex, #span= / #trace?span= deep links, ≤640px vertical timeline with a bottom-sheet drawer, Trace ID box, one live re-fetch after hydration per coverage-21), /trace/[id] using the same viewer (prerender=false, fallback-served, 404 state shows the API's error text), OG/meta tags, placeholder pages for dashboard/logs/uptime/postmortems/alerts/contact, a Playwright smoke suite (8 tests green against both the Vite dev server and the embedded binary), a screenshot script, Makefile web targets and 4 DEVIATIONS lines. Quality gates: svelte-check 0 errors/0 warnings, prettier clean, gen:types stable, build prerenders against the API, go build ./... compiles the embedded dist, and the fresh binary serves the prerendered HTML on /.

### known_gaps

- content/spans.yaml still has the 3 YAML quoting bugs (lines 200 and 418) so `divy serve --content ./content` exits 1; every check here ran against a patched copy under the scratchpad (fix: quote `fix: "capture gate + fingerprinting fixed, alert hygiene"` and `name: "5,000+ active users"`). Until the content lane fixes it, `make web-build` / `make web-e2e` / `make vercel-build` fail at the pass-1 server start.
- No ESLint config (only prettier + svelte-check); no vitest unit tests for hash.ts/format.ts/model.ts; no axe scan or Lighthouse run yet (draft R.9 gates belong to later steps).
- The Go static handler serves 200.html with status 200 for every unknown non-API path (so /nonexistent-page is a 200 + client-side error page; 404.html is only reached when no 200.html exists) — A1's static.go rule, not changed here.
- /trace/[id] is CSR-only (ssr=false, prerender=false): in `vite dev` and the binary it renders after hydration; no server-rendered OG for individual traces.
- Keyboard registry only has the trace scope (j/k/Esc); `/`, `?`, the `promql` sequence and Konami are not wired yet (theme.set('grafana2017') exists; its tokens are placeholders).
- Fonts: the fontsource CSS includes all unicode-range subsets (only the needed woff2 files download); no metric-matched fallback overrides were added — check CLS in the Lighthouse step.
- Minimap brush uses pointer events on SVG rects (svelte-ignore a11y comments) with two range inputs as the keyboard equivalent; no wheel zoom.

### notes_for_next

- Run everything from the repo root. Dev loop: `go run ./cmd/api serve --addr 127.0.0.1:8080 --db file:/tmp/x.db --content <valid content dir> --site-origin http://localhost:5173` then `cd web && npm run dev` (Vite :5173, proxies /api,/metrics,/loki,/healthz,/readyz,/favicon.svg,/og,/robots.txt,/ascii to API_BASE, default http://127.0.0.1:8080). Build: `cd web && API_BASE=http://127.0.0.1:8080 PUBLIC_SITE_ORIGIN=<origin> npm run build` → ../internal/web/dist; then `go build ./cmd/api`. Makefile web section: web-dev, web-build (pass-1 binary on 127.0.0.1:18081, exports PUBLIC_SITE_ORIGIN from SITE_ORIGIN), web-check (svelte-check + prettier), web-gen, web-e2e (builds, serves the embedded site on :18081, `E2E_BASE_URL` → playwright), web-shots (SHOTS_BASE_URL/WEB_SHOTS_DIR).
- Pinned toolchain: @sveltejs/kit 2.70.3, svelte 5.57.0, adapter-static 3.0.10, vite-plugin-svelte 7.3.0, vite 8.2.2, tailwindcss/@tailwindcss/vite 4.3.3, typescript 6.0.3 (NOT 7), svelte-check 4.7.6, json-schema-to-typescript 16.0.0, @playwright/test 1.56.1 (matches the preinstalled chromium-1194 under PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers — do not bump without a matching browser), @types/node 22.20.1, @fontsource-variable/inter + jetbrains-mono 5.3.0, prettier 3.9.6 + prettier-plugin-svelte 4.1.1. `npm install` needs NODE_USE_ENV_PROXY=1 in this sandbox.
- Env wiring: svelte.config.js sets kit.env.dir='..' (root .env), publicPrefix PUBLIC_, privatePrefix API_. Server loads use `$env/dynamic/private` API_BASE via src/lib/server/api.ts `serverApi(fetch)`; browser code uses `$env/dynamic/public` PUBLIC_API_BASE (default '' = same origin) and PUBLIC_SITE_ORIGIN (fallback: origin of postmortems[0].og_image). Never import `$env/static/*` (missing vars fail the build).
- Prerender: root +layout.ts exports prerender=true, trailingSlash='never'; prerender.entries ['*','/404']; handleHttpError ignores API-owned paths (/api/, /loki/, /metrics, /healthz, /readyz, /favicon.svg, /favicon.ico, /og/, /robots.txt, /ascii, /sitemap.xml) because the crawler follows every href — put `rel="external"` on anchors to API URLs anyway. Any route needing a server load MUST live under src/routes/(site)/ (its +layout.server.ts loads {profile, postmortems, siteOrigin}); trace/[id] stays outside that group with no server load (fallback-served) and uses `prerender=false; ssr=false` in +page.ts.
- adapter-static rimrafs internal/web/dist; scripts/postbuild.mjs (part of `npm run build`) recreates dist/.gitkeep. Type gen is `node scripts/gen-types.mjs` (json-schema-to-typescript API, unreachableDefinitions, additionalProperties:false, strictIndexSignatures, tabs/singleQuote) from ../schema/index.schema.json; regenerate after any Go model change (`make gen`), output is byte-stable.
- API client: src/lib/api/client.ts exports createApi({fetch, base}) with trace(id), services(), operations(service), healthz(), readyz(), content.{services,postmortems,postmortem(id),panels,alerts,uptime,manualMetrics,profile,todos}, uptime.heartbeats(days,bucket); `api` = browser instance; ApiError{status,url,errorType?,traceId?} parses Prometheus/plain/Loki-text envelopes; helpers findTag/tagString/tagBool/tagNumber/tagValueText/tagStringList/parseLinks/parseTodos/isTodo/parentSpanId/isTraceId. Add Prom/Loki fetchers here; keep `unknown` narrowing in this file (types.gen.ts gives JaegerKeyValue.value: unknown).
- Trace model: src/lib/trace/model.ts buildTrace(jaegerTrace, nowUs?) → {traceID, roots, nodes (DFS, children sorted by start then name), byId (spanID), byKey (divy.id), startUs, endUs, nowUs, services[{name,title,color,processID,spans}], isContent}; TraceNode has key/name/title/service/color/startUs/endUs/open/plannedEndUs/startRaw/startPrecision/endRaw/endPrecision/todoDates/error/tags(non divy.*)/meta(divy.*)/events/links/postmortems/todos/depth/children/index. snapshotNow(trace) recovers the request time from open spans without a planned end; the viewer uses Date.now()*1000 after mount and refreshes every 60 s. TODO-date spans render hatched with a 'TODO' duration label; the drawer says the interval is inherited from the parent.
- Viewer API: `<TraceViewer trace={JaegerTrace} postmortems={PostmortemSummary[]} />` (src/lib/components/trace/TraceViewer.svelte). Rows are role=tree/treeitem with data-row-id=<spanID> and data-span-key=<divy.id>; drawer is role=dialog with the h2 focused on open; selection writes `#span=<key>` via replaceState and reads `#span=` or `#trace?span=` on mount, on hashchange and on page.url.hash changes (so log lines/postmortems can link to `/#span=<span id>` or the API's `/#trace?span=<id>`). Keyboard: global j/k/Esc via bindKeys('trace', …) in src/lib/keyboard.ts (installKeyboard runs once in the root layout; inputs and [data-keyboard-ignore] are skipped); ↑/↓/Home/End on the tree, Enter/Space/←/→ on rows. Register new global keys with bindKeys(scope, {Key: handler}) — return false from a handler to let the event through.
- Hash codec: src/lib/hash.ts parseHash('#range=7d&layout=…&panel=id') → Record (also parses '#trace?span=x', keeping the section under `_section`), formatHash(state), patchHash(state, patch). The dashboard should read `panel=` from it (alerts runbook_url `/dashboard#panel=commits-weekly`), scroll the panel into view and flash its header.
- Theme/motion/media: src/lib/state/theme.svelte.ts (`theme.current`, set(t), toggle(); localStorage key 'divy.theme'; app.html applies it before first paint; values dark|light|grafana2017 — the 2017 tokens in app.css are placeholders for the Konami step), motion.svelte.ts (`motion.reduced`), media.svelte.ts (`media.narrow` ≤639.98px, `media.hydrated`). Tailwind tokens: bg, panel, panel-2, panel-3, border, fg, fg-muted, fg-dim, green, yellow, red, blue, orange, purple, link, focus (`bg-panel`, `text-fg-muted`, …); fonts `font-sans` (Inter Variable) / `font-mono` (JetBrains Mono Variable) or the `.mono` class; shared chrome classes .panel/.panel-header/.chip/.btn/.btn-primary/.input/.sr-only; CSS animation classes .bar-grow/.fade-in/.slide-in/.slide-up are gated on prefers-reduced-motion: no-preference. Root layout sets --nav-h: 48px and flips <main data-hydrated="true"> after mount (tests wait on it: `page.locator('main[data-hydrated="true"]')`).
- Placeholder routes: src/routes/(site)/{dashboard,logs,uptime,postmortems,alerts,contact}/+page.svelte each render <ComingSoon title=…/> — replace the file, keep the route inside (site) if it needs build-time data. Route metadata for the nav lives in src/lib/nav.ts. Postmortem detail pages should be `(site)/postmortems/[id]/+page.server.ts` with `entries` from api.content.postmortems().
- Playwright: web/playwright.config.ts uses E2E_BASE_URL (default http://127.0.0.1:5173 and auto-starts `npm run dev` with reuseExistingServer) — an API on :8080 must already be running. Spec: web/tests/e2e/trace.spec.ts. Screenshots: `SHOTS_BASE_URL=… SHOTS_DIR=… SHOTS_PREFIX=w1 node scripts/screenshots.mjs`.
- Sandbox facts: content validation currently fails on content/spans.yaml lines 200/418 (quote the two flow-mapping values); the patched copy used here lived only in the scratchpad. The Go static handler serves 200.html (status 200) for unknown non-API paths, so the SvelteKit +error.svelte is what a visitor sees for a bad URL.

## Built the PromQL subset engine (internal/promql: lexer/parser/AST printer/evaluator with Prometheus 3.14 gramm…

**Summary.** Built the PromQL subset engine (internal/promql: lexer/parser/AST printer/evaluator with Prometheus 3.14 grammar, error texts, extrapolatedRate/Kahan/one-to-one matching semantics, 26h default lookback, 11000-point cap, sample/timeout/concurrency guards), the metric catalogue + client_golang registry + live series + staleness-filtered store collector behind /metrics (internal/metrics), the full Prometheus HTTP API under /api/v1 on the chi router (internal/server/promapi.go: query, query_range, series, labels, label values with UTF-8 name validation, metadata, buildinfo, rules from alerts.yaml, alerts, query_exemplars; GET + POST form, exact envelopes/status codes, weak ETag + 304), the Prom* API types in internal/model/prom.go (schema regenerated), the QUERY_* config knobs, the fixed `make promtool-check` target (passes with promtool 3.5), docs/promql-subset.md, and DEVIATIONS lines. Tests: 108 parser rows, 80+ evaluator rows with hand-recomputed expectations, 45+ HTTP rows, promlint + exposition golden, and a test that parses every expr in content/panels.yaml and content/alerts.yaml. The validator now runs the real parser on panel/alert exprs (content.Load defaults CheckExpr to promql.Check); two YAML flow-mapping quoting errors in content/spans.yaml were fixed so `validate --strict` reports 0 errors, 0 warnings. gofmt/vet/golangci-lint clean; every package passes except two pre-existing static-site tests broken by the parallel web lane populating internal/web/dist.

### known_gaps

- No Prometheus oracle module (draft P7): prometheus v0.314.0 needs Go 1.25; expectations were recomputed by hand from the 3.14 source (documented in DEVIATIONS).
- The server-side 15s/60s response cache, X-Cache and X-Divy-Trace-Id are not implemented here (middleware/OTel lanes via server.Hooks); the Prometheus handlers set Cache-Control + weak ETag + 304 themselves.
- divy_otel_spans_total / divy_otel_exported_spans_total / divy_otel_export_errors_total are catalogue entries only; the trace lane must register the actual counters using the catalogue HELP text.
- content/panels.yaml commits-weekly description still says github_commits_total counts contribution-calendar events; the catalogue HELP follows storage S-X1/K-I3 (commits only) — content/collector lanes should align the wording (not a schema-alignment fix, so left untouched).
- The 499 'client closed connection' response writes a JSON envelope with errorType canceled (Prometheus writes nothing); harmless since the client is gone.

### notes_for_next

- internal/promql API: ParseExpr(string) (Expr, error) / Check(string) error / ParseMetricSelector(string) ([]*Matcher, error); errors are *ParseError (Pos, Query, Err; Error() = `line:col: parse error: msg`). Engine{Storage, Live, Lookback, Timeout, MaxSamples, MaxConcurrency}.Instant(ctx, expr, t, Opts{Lookback, Timeout}) (Value, error) and .Range(ctx, expr, start, end, step, opts) (Matrix, error); Value = Vector|Matrix|Scalar|String with MarshalJSON producing the Prometheus `result` JSON and String() in promqltest form. Errors: ErrTimeout (503), ErrCanceled (499), *ExecError (422, incl. ErrTooManySamples), *RangeTypeError (400 invalid parameter query). Storage interface: Select(ctx, []*Matcher, startMs, endMs) ([]SeriesData{Metric Labels incl __name__, Points []Point{T ms, F}}, error) with startMs < t <= endMs; LiveProvider.LiveSeries() []LiveSeries{Metric(), Labels() (no __name__), Value(t) (float64, bool)}. promql.LoadFixture(text, stepMs, baseMs) parses promqltest-style tables (`metric{l="v"} v0 v1 _ v3`). Helpers: ParseDuration/FormatDuration (exact ports of prometheus/common), ParseAPIDuration (float seconds or duration), Labels (sorted slice; NewLabels(map), String() `{a="1", b="2"}`), NewMatcher/MatchLabels (RE2 anchored ^(?s:…)$).
- internal/metrics: Catalogue []Family{Name, Type (counter|gauge|histogram), Help, Labels, Source (SourceStored|SourceLive|SourceProcess), Collector}; Lookup(name), Queryable() (stored+live, what /api/v1/metadata lists), StaleAfter(interval)=max(3×interval,15m), DefaultIntervals{github 15m, pypi 60m, uptime 5m, manual 15m}. Collectors MUST write only catalogue metric names with exactly the catalogue label names (a series outside the catalogue is queryable but never appears on /metrics; wrong label sets still expose but break the golden). NewLive(content, startedAt, version, commit) → *Live implements promql.LiveProvider (divy_uptime_seconds, divy_build_info{version,commit,go_version}, divy_open_to_work, divy_experience_years). metrics.New(Options{Store, Live, Intervals, Now, Logger}) → *Registry with Handler() (/metrics, promhttp, MaxRequestsInFlight 8, Cache-Control no-store), Middleware (divy_http_requests_total{route,method,code} + divy_http_request_duration_seconds{route,method}; route = chi RoutePattern, '/*' fallback; method normalised to GET/HEAD/POST/PUT/PATCH/DELETE/OPTIONS/OTHER), OnResult(name, outcome, dur) (divy_collector_runs_total{collector,result} + divy_collector_run_duration_seconds; 'skipped' is not observed in the histogram), Gatherer(). divy_collector_last_success_timestamp_seconds is read from store.LastSuccess at scrape time. For expfmt.TextParser in tests use expfmt.NewTextParser(model.UTF8Validation) (the zero value panics with prometheus/common v0.66); promlint via promlint.NewWithMetricFamilies(gathered). Golden: internal/metrics/testdata/exposition.golden, regenerate with `go test ./internal/metrics -update`.
- internal/server wiring: server.Config gained QueryLookback/QueryTimeout/QueryMaxSamples/QueryMaxConcurrency, Branch/BuildUser/BuildDate (default from internal/version), CollectorIntervals map[string]time.Duration (main.go passes the five COLLECT_*_INTERVAL values; registered collectors' Interval() and catalogue defaults fill gaps), Metrics *metrics.Registry (nil = built in New). New() sets cfg.Runner.OnResult = registry.OnResult when it is nil, builds the engine with storeQuerier{store} (adapter over store.QueryRange, adds __name__) and mounts: `r.Method(GET, "/metrics", …)` and `r.Route("/api/v1", …)` as a SUB-ROUTER with NotFound=promNotFound and MethodNotAllowed=methodNotAllowed (the old root `/api/v1/*` catch-all is gone: HEAD works through chi GetHead's recursive Match, wrong methods give 405 with Allow). Accessors: s.Metrics(), s.Engine(). Middleware order: recover → Hooks.Outer → securityHeaders → requestContext → metrics.Middleware → requestLog → GetHead → Hooks.Inner → routes. Loki lane: mount /loki/api/v1 the same way (sub-router with its own NotFound/MethodNotAllowed) or its wrong-method requests will hit the `/loki/*` catch-all and answer 404 instead of 405.
- Prometheus handlers (internal/server/promapi.go): every /api/v1 response goes through writeSuccess (weak ETag W/"<16 hex sha256>", If-None-Match → 304, Cache-Control CacheQ15 for query/query_range/series/labels/label values, CacheC60 for metadata/buildinfo/rules/alerts/query_exemplars); errors via writePromError. RuleGroups(content) is exported (Prometheus rule shape; query = canonical parsed form, file = content/alerts.yaml, lastEvaluation = 0001-01-01T00:00:00Z, state inactive, health unknown). Param parsing mirrors Prometheus 3.14 web/api: parseTime (float seconds rounded to ms / RFC3339Nano), parseTimeParam wraps errors as `invalid time value for '<name>': …`, parseLimitParam, ParseAPIDuration; validation order per endpoint copied from api.go. POST bodies: form-urlencoded only (JSON → 400), 1 MiB cap → 413.
- config: QUERY_TIMEOUT (default 30s, ≥1s), QUERY_MAX_SAMPLES (default 2000000, positive int), QUERY_MAX_CONCURRENCY (default 20) added to config.FromEnv and .env.example next to QUERY_LOOKBACK_DELTA=26h; cmd/api/main.go passes them plus Branch/BuildUser/BuildDate and CollectorIntervals into server.Config (only the serve path; `collect` needs nothing).
- content: content.Options.CheckExpr defaults to promql.Check, so `divy validate`/serve now reject unparsable panel targets and alert exprs (rule id panels.expr for both files, as A1 wired it); rows panels-expr-parse and alerts-expr-parse were added to ruleFixtures in internal/content/content_test.go. content/spans.yaml: only the two YAML quoting fixes (line 200 `fix: "capture gate + fingerprinting fixed, alert hygiene"`, line 418 `name: "5,000+ active users"`); strict validation now passes and the TODO count rises to 464 because the fixed spans parse fully.
- Makefile promtool-check: builds bin/divy with LDFLAGS, serves on $(CHECK_ADDR)=127.0.0.1:18081 with --db file:$tmp/promtool.db --log-level warn, waits ≤30s on `divy ping`, pipes /metrics into `$(PROMTOOL) check metrics` (no --extended: promtool 3.5 has no such flag) and runs `$(PROMTOOL) check rules content/alerts.yaml`; the EXIT trap kills the server and removes the temp dir. PROMTOOL resolves to /usr/local/bin/promtool here.
- go.mod: added github.com/prometheus/client_golang v1.23.2 (direct; brings prometheus/common v0.66.1, client_model, procfs, protobuf as indirect); `go mod tidy` keeps `go 1.24.0`. Run everything with GOTOOLCHAIN=local.
- Ports/paths used and released: 127.0.0.1:18081 and /tmp/divy-a2.db (deleted). No background processes left.
- Draft rows corrected by the implementation (also in DEVIATIONS): E30 duplicate-matching error text/order, H6 time-param message, P.7.3 `30`/`60` sample values (29.999999999999996 / 59.99999999999999), H13 label-name validation (protocol-03), lookback 26h (protocol-02).
- Web/static lane: internal/server TestIndexNegotiation and TestContentEndpoints assume internal/web/dist is empty; make them conditional on web.HasIndex() (or clean dist before `go test`) now that the web build writes into the tree.


## Orchestrator additions after round 1

- `vercel.json` (framework go, `buildCommand: bash deploy/vercel-build.sh`, `functions["cmd/api/main.go"].maxDuration=60`, daily cron on `/api/collect`) and `deploy/vercel-build.sh` (two-pass build writing `$VERCEL_OUTPUT_FILE`) exist and were verified locally: the produced binary runs from an empty working directory with `PORT` + `VERCEL=1` only.
- `embedded.go` at the module root (`package site`, import path `divy.dev`) embeds `content/`; `cmd/api` `resolveContentDir` extracts it to `$TMPDIR/divy-content-<commit>` when `--content` does not exist (or equals `embedded`). Local runs keep using `./content`.
- `internal/config.resolveDBURL`: when `VERCEL` is set and no DB URL is configured, the store is `file:/tmp/divy.db` (ephemeral). Later agents: surface this as `storage: ephemeral` in `/readyz` and the UI.
- `server.Config.SiteFS` overrides the embedded site (tests pass `fstest.MapFS{}` so they do not depend on `internal/web/dist`).
- `config.SiteOrigin` defaults to `https://$VERCEL_PROJECT_PRODUCTION_URL` (then `VERCEL_URL`) when `SITE_ORIGIN` is unset.
