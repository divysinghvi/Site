# Build round 2 hand-off notes (collectors, dashboard, LogQL/Loki, postmortems/contact/uptime)

## Built the five real collectors (GitHub GraphQL with 365-day commits/contributions backfill, resumabl…

**Summary.** Built the five real collectors (GitHub GraphQL with 365-day commits/contributions backfill, resumable merged-PR scan with privacy rule, stars/followers/open-PR gauges, rate-limit back-off; PyPI pypistats 180-day backfill + pypi.org version with ETag; uptime prober with error classes, ≤5 redirects, budget-cut gaps; manual gauges + updated timestamps; retention), the shared daily-grid/batch/gauge-policy helpers and cadence-gated /api/collect rounds (`RunRoundOpts{OnlyDue}`, `?force=1`), GET /api/uptime + /api/uptime/heartbeats (contract shape, incidents ≥2 probes, null windows, weak ETag), GET /favicon.svg (exact L.6.3 sparkline, no-data baseline) and a cacheable 404 /favicon.ico, /readyz storage kind (file|libsql|ephemeral) and real collector rows, CLI wiring (`serve --collect`, `collect --once`, warning without token), docs/collectors.md and 10 DEVIATIONS lines. All Go gates green (gofmt, vet, golangci-lint 0 issues, go test ./... incl. httptest suites for every query path, pagination, privacy, errors, probes), `make promtool-check` passes, schema regenerated and drift-free. Live check: GitHub skipped (no token), pypistats blocked by the sandbox (error recorded, pypi.org version 0.2.0 written), uptime probes pypi.org up / self up / github.com 403 (sandbox egress, recorded honestly as down), manual and retention ok.

### known_gaps

- The GitHub collector could not be exercised against the real API here (api.github.com answers 403 in the sandbox); every query path is covered by httptest fakes with docs.github.com-shaped bodies. Whether private-repository commits appear in Q1 for the token owner remains to be read off the first real run's debug line (`github contributions window … restricted=N has_restricted=…`).
- pypistats.org is blocked in the sandbox, so pypi_downloads_total was only verified through the httptest fixture; the live run records the error honestly and /readyz shows pypi.ok=null until it succeeds.
- github-profile shows `down` (403) in this sandbox because the egress proxy denies github.com/divysinghvi for GET and HEAD alike (curl confirms); outside the sandbox the profile answers 200.
- collector.Batch commits sample writes through store.Write, which does not bump store.Generation() (only EnsureSeries/WriteProbeResults/DeleteSeriesWhere do); nothing reads Generation() yet — the store owner should bump it inside Write or export a Bump for the future response cache (noted in DEVIATIONS).
- Uptime `up` rule follows the task (2xx/3xx when no expected_status is listed) while /api/content/uptime (internal/content TargetView, not mine) still displays the content default `expected_status: [200]` for such targets — cosmetic inconsistency, documented in DEVIATIONS.
- The whole /api/* family answers 404 (not 405) to wrong methods because of the pre-existing `/api/*` catch-all in server.go; my routes inherit that (the contract's K-X5 405 is a foundation-lane fix).
- web/src/lib/api/types.gen.ts is now behind schema/ (HeartbeatTarget.note, ReadyzDB.storage) until the web lane runs `make web-gen`; `make gen-check` will flag it until then.

### notes_for_next

- Packages: internal/collector (framework + helpers), internal/collector/{github,pypi,uptime,manual,retention}. Registration lives in cmd/api/main.go `registerCollectors(cfg *config.Config, cnt *content.Content, st *store.Store, log *slog.Logger) *collector.Registry` (order github,pypi,uptime,manual,retention; COLLECT_DISABLED → collector.WrapDisabled). `collector.Process{}` still exists for tests but is no longer registered by the binary.
- Framework additions (internal/collector/collector.go): `RoundOptions{Budget, Names, OnlyDue, Now}` + `Runner.RunRoundOpts`; `RunRound` unchanged (= OnlyDue false, used by `collect --once`); `DueSlack(interval)=min(interval/10,1m)`; a not-due collector yields CollectorResult{OK:false, Error:"skipped: not due (last success X ago, interval Y)"} and OnResult(name,"skipped",0), no collector_runs row. `WrapDisabled(c, reason)` → Disabled() true, Run returns ErrDisabled wrapped ("skipped: <reason>").
- Shared helpers (internal/collector/series.go): DayMs, DayKey/DayOf/DayEnd/IsGrid, DailyCounts{Add,First,Total,SortedKeys}, `CounterSamples(counts, w0, now, base, haveBase) (grid, live)` (storage §S.2.3 incl. rule-4 frozen prefix), `LoadExisting(ctx, st, metric, sinceMs, untilMs) Existing` (one QueryRange per metric) with GridValue/Changed, `Batch{Upsert, DeleteOffGrid, Counter(id, ex, grid, live), Gauge(ctx, idx, metric, labels, value, now), Commit(ctx) (items, err)}` (one store.Write tx; deletions before upserts; rejects NaN/Inf), `LoadLatest(ctx, st) LatestIndex` + `GaugeDue` (write iff changed or ≥1h). Also `collector.UserAgent(origin)` = "divy.dev-collector/<version> (+origin)", `collector.NewHTTPClient(timeout)`.
- GitHub (internal/collector/github): `New(Config{Token, Login, PrivateOrgs, Interval, Endpoint, UserAgent, HTTPClient, MaxPRPages(5), MinRemaining(200), RetryDelay(2s), Now, Logger}, st)`; Disabled() when Token==""; metrics constants MetricCommits/MetricContributions/MetricMergedPRs/MetricMergedByRepo/MetricStars/MetricFollowers/MetricOpenPRs; `StateKey="github.prs"` (JSON prState{windows,i,after,events[{d,o,r?}],started_ms,pages,done_ms,count}); sentinel errors ErrTokenRejected/ErrRateLimited; run note e.g. `commits_365d=27 contributions_365d=33; prs=5 (scan complete, 2 pages); gh_remaining=4990 gh_reset=…` or `prs scan in progress (window i/n, E events so far)`. Fixture-driven tests in github_test.go (fakeGitHub dispatches on operation name Contributions/MergedPRs/OpenPRs/Repos; window filtering by from/to variables).
- PyPI (internal/collector/pypi): `New(Config{Packages, Interval, StatsBase, PyPIBase, UserAgent, HTTPClient, Now, Logger}, st)`; `PackagesFromContent(c)` reads spans links kind=pypi (https://pypi.org/project/<name>/); `MergePackages(lists...)`; state keys `pypi.etag.<pkg>`, `pypi.version.<pkg>`; run error lists `<pkg> downloads: …` / `<pkg> info: …`, Items counts what was written anyway.
- Uptime (internal/collector/uptime): `TargetsFromContent(c, maxTimeout) []Target` (Target{ID,Name,URL,Configured,Method,Expected(nil = 2xx/3xx),Timeout,Interval,FollowRedirects}); `New(Config{Targets, Interval, UserAgent, Concurrency(5), Now, Logger, Transport}, st)`; `UserAgent(origin)`="divy-uptime/1.0 (+origin)"; per-target due check via store.LastProbe (interval−10s); error classes ClassDNS/TLS/Timeout/Conn/HTTP/Redirect/Read/Other stored as "<class>: <msg>" (query/userinfo stripped, ≤200 chars); `Classify(err)`; probes cut by the parent ctx record nothing (note cut_by_budget=N, run error wraps ctx.Err()). Writes probe_results via store.WriteProbeResults + probe_success/probe_duration_seconds/probe_http_status_code samples at the probe ts.
- Manual (internal/collector/manual): `New(Config{Metrics(nil → c.Manual.Metrics), Interval, Now, Logger}, c, st)`; MetricUpdated="divy_manual_metric_updated_timestamp_seconds"; TODO updated_at → no timestamp series (note updated_at_todo=…). Retention (internal/collector/retention): `New(Config{Interval, Now, Logger}, st)`; Items = rows deleted/marked; note per-table counts or `nothing expired`.
- Server: routes added in server.go buildRouter (inside the timeout group): GET /api/uptime, /api/uptime/heartbeats, /favicon.svg, /favicon.ico. `server.Config.Storage model.StorageKind` (file|libsql|ephemeral) → /readyz checks.db.storage; main.go `storageKind(st)` uses VERCEL env. handlers_uptime.go exports `(*Server).Heartbeats(ctx, now, days, bucket) (model.UptimeHeartbeats, error)`; incidents need ≥2 consecutive failures (const uptimeMinIncident); windows longer than `days` are null; buckets only for periods with probes. handlers_favicon.go: `faviconSVG(counts [7]float64, have bool, from, to)`, `(*Server).commitCounts`. /api/collect now uses RunRoundOpts{OnlyDue:true} unless `?force=1|true`; existing TestCollect in server_test.go was updated accordingly (POST runs, GET → skipped: not due, GET ?force=1 runs).
- Model changes (additive): `model.ReadyzDB.Storage StorageKind json:"storage,omitempty"` (new enum StorageKind file|libsql|ephemeral in enums.go) and `model.HeartbeatTarget.Note *string json:"note" nullable`. schema/ regenerated (`go run ./cmd/api schemagen --out ./schema`). WEB LANE: run `make web-gen` (or `cd web && npm run gen:types`) to refresh types.gen.ts; the uptime page can read `note`, `status`, `last`, `uptime.{24h,7d,30d,90d}` (null = no data → grey, never green), `buckets[]{ts,samples,up_ratio,avg_latency_ms,max_latency_ms}` (absent days = grey), `incidents[]`. Exact JSON in docs/collectors.md.
- CONTENT LANE (do not edit content from here): content/panels.yaml `commits-weekly` description still says github_commits_total counts contribution-calendar events; the collector counts commits only (commitContributionsByRepository.commitCount) — the calendar count is `github_contributions_total`. Also content/alerts.yaml HighContributionRate summary should say commits, and its `20 contributions/week` comment refers to commits now. Optionally reword `/api/content/uptime` default expected_status ([200] via content.TargetView) to match the prober's 2xx/3xx default, or add explicit `expected_status: 200` to targets.
- STORE LANE: consider bumping Generation() inside Store.Write (or exporting Bump) — collector.Batch writes samples through Write and does not bump; nothing reads Generation() today. A `Store.WriteBatch`/Tx helper would let the collectors drop their raw SQL (`INSERT INTO samples … ON CONFLICT`, `DELETE … ts_ms % 86400000 != 0`) which mirrors data.go.
- Cadence semantics on Vercel: collect.yml fires every 5 min; uptime runs every round (5m), GitHub every 15m, manual 15m, PyPI 60m, retention 60m via the due check (last success from collector_runs; a failed collector stays due and retries every round — e.g. pypi while pypistats is blocked). Slack min(interval/10,1m) tolerates late crons. Per-collector timeout in a round = min(RunTimeout(interval), budget) = 8s by default; GitHub phases commit independently (Q1 counters → gauges → Q2 scan pages persisted after each page) so a timeout keeps progress.
- Write-budget note for Turso: backfills write only changed grid points (LoadExisting diff), steady state ≈ 2 rows per counter series per run + probe rows; reads are one QueryRange per metric per run.
- Ports/paths used and released: 127.0.0.1:18082 (my server), 18083 (promtool-check), 18084 (VERCEL=1 check); /tmp/divy-a4 binary still exists (harmless), /tmp/divy-a4.db deleted; scratchpad logs a4-serve.log/a4-serve2.log/a4-collect.log/a4-vercel.log.
- The sandbox egress returns 403 for https://github.com/divysinghvi (GET and HEAD) and denies CONNECT to pypistats.org; do not treat a `down` github-profile or a failed pypi run here as a bug.

## W2 done: /dashboard renders content/panels.yaml on a 24-column grid (percentage-placed cells, custom…

**Summary.** W2 done: /dashboard renders content/panels.yaml on a 24-column grid (percentage-placed cells, custom pointer drag-to-rearrange + resize with collision push-down/compaction, keyboard Move/Resize via the kebab menu, layout+range+refresh in the URL hash, #panel=<id> scroll+focus+flash, Reset layout), panel types timeseries (uPlot 1.6.32: synced crosshair key `divy-dashboard`, HTML tooltip, legend toggles incl. dbl-click isolate, cumulative-sum stacking with bands for merged-prs-by-org, units, dashed threshold lines, CSS clip draw-in gated on reduced motion), stat (value from the query, unit/decimals/thresholds, sparkline from a range query when graph_mode=area, manual "source: manual · last updated <date|unknown>" badge from the hidden companion target), gauge (SVG arc with threshold markers) and bargauge; time range picker 24h/7d/30d/1y/all (all = career root span start from the page's server load) with step = max(preset floor, ≤1000 points), auto-refresh 30 s toggle (visibility-aware), View query dialog (PromQL per target, resolved start/end/step, copyable GET curl, focus trap, Open in Explore), honest empty states built from /readyz collectors (e.g. "github: collector disabled — no DIVY_GITHUB_TOKEN configured", "pypi: no successful collector run yet"), ≤640 px single column with a <select> picker. /explore?ds=prom&expr=…&from=…&to=…[&instant=1]: Prometheus/Loki tabs (Loki reserved: "Logs explorer lands in the next step"), PromQL combobox autocomplete (metric names from /api/v1/label/__name__/values with metadata help, labels/values from /api/v1/labels and /api/v1/label/{name}/values, functions/aggregations/keywords/ranges from a static list mirroring docs/promql-subset.md), range picker, Range/Instant, Run, graph (uPlot) or table, API error text, curl line, `/` focuses the bar; every panel's Explore menu item links here. Gates: svelte-check 0/0, prettier clean, 25/25 Playwright tests (dashboard 9, explore 8, trace 8) against the Vite dev server + real API, `npm run build` prerendered against the API, `go build ./...` with the fresh dist, embedded binary serves /dashboard (12 panel ids) and /explore. Samples seeded with `divy collect --once` (manual + uptime + pypi_package_info); GitHub disabled (no token) and pypistats blocked here, so the screenshots show the honest empty states.

### known_gaps

- web/src/lib/api/types.gen.ts is stale vs schema/index.schema.json (no Prom* types): the four resultType shapes are narrowed locally in web/src/lib/panels/prom.ts with a local PromResultType alias; `make gen` / `make gen-check` will regenerate and the drift guard may currently fail (not my file to regenerate).
- No live data for GitHub/PyPI-download panels in the sandbox (token empty, pystats blocked): timeseries stacking/bands/tooltips were verified only via a test-only route mock; bargauge (stars-by-repo) rendered only in the empty state.
- Panel titles at w=6 on 1440 px still truncate slightly next to the badge ('Active us…'); the container query (<400 px) shortens the manual badge and hides the source chip but does not shrink further.
- No axe scan / Lighthouse run in this step (draft R.9 gates belong to a later step); no vitest units for timerange/grid/hash-layout (Playwright covers the round trips).
- The sandbox Chromium reports navigator.language 'en-US@posix'; uPlot's import-time Intl.NumberFormat throws on it, so the new specs and tests/shots/w2.mjs pin locale 'en-US' and UPlotChart shows a red 'chart library failed to load' status if that ever happens in a real browser.
- Auto-refresh uses the task's 30 s, not panels.yaml dashboard.refresh (60s) — recorded in DEVIATIONS.

### notes_for_next

- Dashboard route: web/src/routes/(site)/dashboard/{+page.server.ts,+page.svelte}; server load returns {panels: PanelsFile, allFrom?: unix seconds of the earliest career span}. Explore: web/src/routes/(site)/explore/{+page.server.ts,+page.svelte} (load returns {allFrom}); both prerender (dashboard.html, explore.html) and read hash/query params after hydration only.
- URL contracts: /dashboard#range=<24h|7d|30d|1y|all>&layout=<base64url JSON {id:[x,y,w,h]} of overridden panels only>&refresh=off&panel=<id> (default range and refresh=on are omitted; unknown keys preserved) — codec in web/src/lib/hash/layout.ts (parseDashboardHash/formatDashboardHash/diffLayout/encodeLayout/decodeLayout) on top of W1's $lib/hash. Explore: /explore?ds=prom|loki&expr=…&from=now-7d|<unix s>&to=now|<unix s>[&instant=1]; helpers presetToRelative/parseTimeParam/presetFromParams in web/src/lib/timerange.
- Time range: web/src/lib/timerange/index.ts — resolveRange(preset, {allFrom, nowMs}) → {preset, from, to, step}; chooseStep(from, to, floor) picks the smallest of [1m…30d] ≥ PRESET_MIN_STEP (24h→5m, 7d→1h, 30d→6h, 1y→1d, all→1d) keeping ≤ MAX_POINTS=1000; humanStep(), formatStep(). Dashboard steps here: 24h=300, 7d=3600, 30d=21600, 1y=86400, all=172800 (2.3y/1000).
- Prometheus client: web/src/lib/panels/prom.ts — query(expr,{time,signal}), queryRange(expr,{from,to,step,signal}) → {data: PromData (discriminated on resultType), warnings}; labelValues(name, match[]), labelNames(match[]), series(match[]), metadata(); queryPath/queryRangePath (GET URLs), curlRange(origin, expr, from, to, step)/curlInstant(origin, expr) (multi-line `curl -sG … --data-urlencode`), legendName(format, metric) ({{label}} templating), formatLabels, sampleValue. Errors are ApiError from $lib/api/client. W1's client.ts was not touched (not owned): if you fold these into client.ts, keep the narrowing in one place.
- Panel runtime: web/src/lib/panels/model.ts — runPanel(panel, range, signal) runs every target (hidden ones too; `hide` = run but do not draw, coverage-14) → PanelRun {results[], spark?, fetchedAt}; helpers statValues, sparkPoints, latestValue, isEmpty, firstError, lastUpdated(panel, run) (manual companion series → {ts|unknown}), graphMode/colorMode/showThresholdMarkers from panel.options; sourceCollector(kind), collectorForMetric(name), collectorStatus(name, readyz) → {state: missing|disabled|never|stale|ok, text}, emptyMessage(kind, readyz), emptyMessageForExpr(expr, readyz), fetchReadyz(force) (10 s cache). In-flight dedup shares one fetch per URL with a subscriber-counted AbortController (do not pass per-caller signals into fetch directly).
- Readyz collector names seen here: github (disabled:true when DIVY_GITHUB_TOKEN empty), pypi, uptime, manual, retention, process. Empty-state wording keys off those names; the alerts page (W4) can reuse collectorStatus for its own 'why is this rule inactive' hints.
- Grid: web/src/lib/panels/grid.ts — COLS 24, ROW_H 30, MARGIN 8, MIN_W 3, MIN_H 2; place(items, id, pos) = clamp + push-down of overlapped items + upward compaction (gridstack float:false semantics); nudge(items, id, dx, dy, dw, dh) (vertical moves swap with the nearest panel in the same columns); pixelRect/pixelHeight/colWidth/snapX/snapY. DashboardGrid.svelte places cells with `calc(x * (100% + 8px) / 24)` so the prerendered HTML is laid out without measuring; ≤640 px CSS turns it into one column (cell height --narrow-h, h capped at 10 rows) and the page passes draggable=false. Exports CellApi {dragStart, resizeStart, active} to the `cell` snippet.
- Components: Panel.svelte frame (article[data-panel-id][data-panel-type] tabindex=0 aria-labelledby/-describedby, grip button 'Drag to move <title>', info tooltip role=tooltip, .chip.src / .chip.manual badges with long/short variants via container query, resize button 'Resize <title>', PanelMenu with MenuItem {id,label,href?,action?,separator?,hidden?}); UPlotChart.svelte props {data: ChartData, unit, decimals, stacked, syncKey, thresholds[{value,color}], legendVisible (bindable), label, xRange:[from,to]} — always pass xRange or sparse results get an auto x-scale; imports uplot/dist/uPlot.min.css; theme colours re-read on theme.current change (canvas needs resolved colours: readTheme() in charts/uplot.ts). Sparkline.svelte {points, color, height}. QueryInspector.svelte {open (bindable), title, entries[{refId,expr,kind,hidden}], range, origin, exploreHref}. QueryBar.svelte {value (bindable), metrics, meta, fetchLabels(metric?), fetchValues(label, metric?), onrun, id} exposes focus(); autocomplete logic in promqlComplete.ts (contextAt/suggest/apply, FUNCTIONS/AGGREGATIONS/KEYWORDS/RANGES lists — extend when the PromQL subset grows). TimeRangePicker.svelte {value (bindable), options, name, label} renders both the segmented radios and a <select> (CSS picks at 640 px).
- Units: web/src/lib/panels/units.ts — formatValue(n, unit, decimals) for short|none|percent|s|dtdurations, formatAxis, thresholdColor(value, thresholds, min, max) → PaletteColor, paletteVar(), SERIES_COLORS fixed order green/blue/orange/purple/yellow/red (brief §5 palette; validated CVD-safe against #181b1f, lightness-band check fails by design of the brief's palette).
- Curl origin = data.siteOrigin from the (site) layout (PUBLIC_SITE_ORIGIN, else the og_image origin) falling back to location.origin in the browser; here http://127.0.0.1:8080.
- Tests: web/tests/e2e/{dashboard,explore}.spec.ts use test.use({locale:'en-US'}) (sandbox navigator.language 'en-US@posix' breaks Intl at uPlot import) and need the API on :8080 (+ Vite :5173 auto-started by the config). Screenshot script: PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers SHOTS_BASE_URL=http://127.0.0.1:5173 SHOTS_DIR=<dir> node web/tests/shots/w2.mjs (SHOTS_PREFIX default w2).
- Dependency added: uplot 1.6.32 (web/package.json, exact). No gridstack (custom grid, see DEVIATIONS). Chart code loads uPlot via dynamic import inside onMount so prerender never executes it; a failed import shows a role=status error instead of a blank panel.
- Keyboard: panels are tab stops; kebab = button aria-haspopup=menu → role=menu with ↑/↓/Home/End/Esc; dialog traps focus, Esc closes, focus returns to the opener; Explore binds `/` via bindKeys('explore', …). Global `?`/promql/Konami are still unbound (W4).
- For W4a (logs): the Explore Loki tab keeps ds=loki in the URL and renders the reserved message in web/src/routes/(site)/explore/+page.svelte (`{#if ds === 'loki'}` block) — replace that block with the LogQL query bar; QueryBar.svelte's combobox/listbox pattern and promqlComplete.ts's contextAt shape can be cloned for LogQL. For W4b (console): promqlComplete.FUNCTIONS/AGGREGATIONS and prom.query()/formatLabels are what the floating console needs; link out with /explore?ds=prom&expr=…&from=now-7d&to=now.
- API lane: /readyz already exposes checks.db.storage (ephemeral|file|libsql) per their DEVIATIONS line — the dashboard does not surface it yet (could go next to 'refreshed hh:mm:ssZ' in the toolbar). types.gen.ts should be regenerated (`make gen`) so prom.ts can import PromResultType/PromQueryResult instead of the local alias.

## Built the LogQL subset engine (internal/logql: lazy lexer, recursive-descent parser with Loki-format…

**Summary.** Built the LogQL subset engine (internal/logql: lazy lexer, recursive-descent parser with Loki-format errors and canonical String() printing, `| json` extraction with the protocol-04 collision rule, string/numeric/duration/bytes label filters with LabelFilterErr semantics, count_over_time/rate + sum/count/min/max/avg by/without, vector() constant folding, in-memory Store with labels/values/series/stats/volume, Loki-exact JSON marshalling) and the Loki HTTP API (internal/server/handlers_loki.go: GET+POST form query_range, query, labels, label values, series, index/stats, index/volume, status/buildinfo as a /loki/api/v1 sub-router with text 404/405; protocol-01 timestamps, protocol-05 step=…ms / no-limit metric queries, protocol-06 limit text, protocol-11 encoding flags ignored; Q15/C60 Cache-Control, weak ETag + 304, 5 s eval timeout → 504). Added the content stream indexer (internal/content/streams.go: LogStreams, LogsStart), the Loki response types in internal/model/loki.go + APIRoots (schema/ regenerated), docs/logql-subset.md (final subset, endpoint table with real bodies, Grafana data-source steps and Explore examples on the real content) and 8 DEVIATIONS lines. Tests: 63 parser rows, ~40 evaluator rows on the 11 fixture lines plus synthetic JSON/bytes/duration lines, 4 HTTP tests with 13 error rows and 5 timestamp forms. Gates: gofmt, go vet, golangci-lint (0 issues), schemagen --check, validate --strict, make promtool-check (port 18083) all green; go test ./... green for every package except the pre-existing race in internal/collector/github TestErrors (reproduces without my changes). Live run on 127.0.0.1:18083 exercised every endpoint against content/ (Grafana health check returns "2"; `{service="gradr"} |= "sentry" | json | level="warn"` returns the three Sentry incident warnings). Server killed, DB and binary removed.

### known_gaps

- `| json` skips arrays (and nulls) exactly as Loki's no-argument parser does and as logql.md §L.1.6 specified; the task text said "arrays indexed" — not implemented (no content line has an array; documented in docs/logql-subset.md §3.2). Say so if `c_0`, `c_1` style labels are wanted.
- No request-level response cache / X-Cache, no X-Divy-Trace-Id/X-Divy-Trace-Sampled, no per-IP rate limit on /loki/*: those middlewares belong to the OTel/cache lanes (server.Hooks); the 500 body is the recoverer's JSON `{"error":"internal error: <request id>"}`, not Loki text with a trace id.
- 405 responses list `Allow: GET, POST` (no HEAD/OPTIONS) — the pre-existing methodNotAllowed probe in server.go, shared with the Prometheus family.
- Timestamps in matrix/vector output are millisecond precision (Loki's model.Time); a step that is not a whole millisecond truncates.
- Grafana was not run against the API here (no Grafana in the sandbox); every request shape in docs §6 is taken from the grafana-loki-datasource source cited by the review findings and pinned by handlers_loki_test.go, but review protocol-11's "run one Explore query against a real Grafana ≥ 12.3" remains to be done by Divy.
- web/src/lib/api/types.gen.ts is behind schema/ (Loki* types) until the web lane runs `make web-gen`; `make gen-check` flags it until then.
- `bin/divy` was rebuilt by make promtool-check (git-ignored).

### notes_for_next

- A3: Package internal/logql (docs/logql-subset.md). API: `Parse(string) (Query, error)` → *LogQuery | *MetricQuery | *ScalarQuery, `ParseLogQuery`, `ParseSelector(string) ([]*Matcher, error)` (selector only; stages → `parse error : syntax error: only a stream selector is allowed here`); errors are *ParseError{Line, Col, Msg} printing `parse error at line L, col C: msg` or `parse error : msg` when Line=Col=0 (empty query, non-empty-selector rule). `Query.String()` is the canonical form (round-trips). AST: LogQuery{Selector []*Matcher, Stages []Stage(LineFilter|JSONParser|LabelFilter{Expr LFExpr(LFOr|LFAnd|LFString|LFNumber{Kind NumPlain|NumDuration|NumBytes, Value seconds/bytes, Text})})}, MetricQuery{Agg *Aggregation{Op, Grouping, Without, Labels}, Range *RangeAgg{Fn count_over_time|rate, Log, Range, RangeText}}, ScalarQuery{Value}. Reserved words (not label names): json and or by without count_over_time rate sum count min max avg vector.
- A3: Evaluation: `logql.NewStore([]Stream{Labels, Entries []Entry{TS ns, Line}})` (sorts), `Store.Select/LabelNames/LabelValues/Series/Stats/Volume(sel, start, end ns, …)` with window start ≤ ts < end; `Engine{Store}.Logs(ctx, *LogQuery, LogOptions{Start, End, Limit, Forward}) (Streams, Stats, error)`, `.Range(ctx, Query, start, end ns, step) (Matrix, Stats, error)` (steps t=start+k·step ≤ end, >11000 steps → *QueryError, >1000 series → *QueryError, ctx errors returned as-is), `.Instant(ctx, Query, t ns) (Vector, Stats, error)`; `(*LogQuery).Process(streamLabels, line) (Labels, bool)` is the per-entry pipeline. Streams/Matrix/Vector implement MarshalJSON in Loki's exact shapes (values[i][0] ns string; sample ts = float seconds via FormatTime(ms); values via FormatValue: 'f' -1, +Inf/-Inf/NaN). Labels is a sorted []Label with Get/Has/With/Keep/Without/Map/String (`{a="1", b="2"}`). Constants ErrorLabel/ErrorDetailsLabel/ErrJSONParser/ErrLabelFilter, MaxSteps=11000, MaxSeries=1000.
- A3: internal/server/handlers_loki.go: `(*Server).mountLoki(r chi.Router, timeout)` is called from buildRouter right after the /api/v1 sub-router (server.go, the only line added there); it builds `lokiAPI{engine, start}` from `content.LogStreams()`/`content.LogsStart()` at New() time. Routes: GET+POST query_range, query, labels, label/{name}/values, series, index/stats; GET index/volume, status/buildinfo; sub-router NotFound = lokiNotFound (`not supported by divy.dev; see /loki/api/v1/status/buildinfo`, const lokiNotSupported), MethodNotAllowed = methodNotAllowed (text). Constants lokiDefaultLimit=100, lokiMaxEntries=5000, lokiTimeout=5s. Param helpers: parseLokiTime (protocol-01), bounds (end=now, start=since or root start), limitParam, maxEntries (log queries only), directionParam, stepParam (default max(floor((end-start)/250),1)s; ParseAPIDuration accepts `15000ms`), queryParam, selectorParam. Errors go through writeText (text/plain; charset=utf-8, nosniff, no-store). Success via lokiAPI.writeJSON(w, r, cache, v, etagOf) — query responses pass resultType+result bytes as etagOf so If-None-Match → 304 despite execTime.
- A3: internal/content/streams.go (new): `(*Content).LogStreams() []LogStream{Labels map[string]string, Key, Entries []LogEntry}` sorted by Key, entries ascending by TSNano; `(*Content).LogsStart() time.Time` = root span resolved start (fallback 2023-01-01). Content ordering timestamps: L7-L9 of the fixture are 2026-03-01T00:00:00Z + 6/7/8 ns, so an instant query at `time=1772323200` (exact second) excludes them — tests rely on this.
- A3: MODEL/SCHEMA: internal/model/loki.go adds LokiResultType (streams|matrix|vector), LokiResult (json.RawMessage with oneOf schema: streams items {stream, values [[string,string]]}, matrix {metric, values [[number|string,…]]}, vector {metric, value}), LokiStats{Ingester, Store, Summary} (Summary has totalEntriesReturned), LokiQueryData/LokiQueryResult (one type for both query endpoints), LokiLabelsResult, LokiSeriesResult, LokiIndexStats (no envelope), LokiVolumeData/LokiVolumeResult, LokiBuildInfo (no envelope); APIRoots gained these 6 roots; `schema/` regenerated and drift-free. WEB LANE: run `make web-gen` (cd web && npm run gen:types) to get the Loki types; then build the logs page on `LokiQueryResult` (narrow `data.result` on `data.resultType`) — ApiError already treats non-JSON /loki bodies as the message.
- A3: Loki-vs-draft behaviours to remember: `| json` collision → equal value skipped, different → `<key>_extracted` (Loki would always suffix); numeric filter on a missing label drops the entry silently; an entry carrying __error__ passes numeric filters (only string filters drop it); bytes literals supported; nested aggregations rejected; `end < start` → `end must be after start`; `limit` cap 5000 only for log queries; default window = root span start..now; instant log query = all entries with ts ≤ time newest first. Grafana verbatim requests pinned in handlers_loki_test.go: health check `GET /loki/api/v1/query?direction=backward&query=vector(1)%2Bvector(1)&time=4000000000` → `[4000000000,"2"]`; range `start/end` 19-digit ns, `step=<n>ms`, no limit for metric queries.
- A3: go.mod: `go mod tidy` promoted github.com/dustin/go-humanize v1.0.1 (used by logql for bytes literals) and github.com/prometheus/common v0.66.1 (already imported directly by internal/metrics tests) from indirect to direct; go.sum unchanged. Run with GOTOOLCHAIN=local.
- A3: COLLECTOR LANE (not mine): internal/collector/github TestErrors is racy — see checks_failed for the exact cause (client.go:105 `%s` wrap defeats errCanceled at github.go:394) and fix.
- A3: Ports/paths used and released: 127.0.0.1:18083 (live run and promtool-check); /tmp/divy-a3 and /tmp/divy-a3.db* deleted; scratchpad log a3-serve.log kept; no background processes left. README.md does not exist yet at the repo root — the Loki data-source paragraph for the README lives in docs/logql-subset.md §6.1 for the deploy/docs lane to copy.

## W3 done: /postmortems (list, newest first, severity badge with the SEV1–SEV4 definitions tooltip, TO…

**Summary.** W3 done: /postmortems (list, newest first, severity badge with the SEV1–SEV4 definitions tooltip, TODO dates literal, service chips, span links), /postmortems/[id] (prerendered per id via `entries`, sanitized API HTML in a monospace document with `##` heading markers, sticky scroll-spy TOC that collapses to a <details> bar under 900 px, metadata header, "View in trace" → /trace/career?span=<id>, prev/next, per-report OG/meta from the API's og_image), /contact as a runbook (escalation table, channels with literal TODO(divy) badges, copyable `curl <origin>/healthz` with the live response + X-Divy-Trace-Id when present, open-to chips, Asia/Kolkata clock), /uptime as an Uptime-Kuma status page over /api/uptime (per target: state with red-when-down, latency, last probe, 24h/7d/30d/90d with null → "—", 90 daily cells with absent days grey, incident table with error class, the self-probe note, unconfigured TODO targets grey and excluded from the honest banner; 60 s visibility-aware auto-refresh; uptime-collector status from /readyz). TraceViewer now consumes `?span=<id>` into `#span=`. Regenerated types.gen.ts (HeartbeatTarget.note, ReadyzDB.storage, Prom* types). Gates: svelte-check 0/0, prettier clean, `npm run build` prerenders all four INC pages, `go build ./...` with the fresh dist, Playwright 36/36 on Vite and the 11 W3 tests against the embedded binary; screenshots w3-{postmortem-1440,postmortem-390,contact-1440,uptime-1440,uptime-390,postmortems-list-1440}.png in the scratchpad shots dir. Five DEVIATIONS lines appended.

### known_gaps

- Unknown postmortem ids on the embedded binary show SvelteKit's '500 Internal Error' page: the Go static handler answers 200.html with status 200 for /postmortems/INC-999 AND for /postmortems/INC-999/__data.json, so the client data load parses HTML as JSON. The spec accepts /404|not found|500/ and asserts no report renders; the fix is in internal/server/static.go (API lane): answer 404 for a missing __data.json.
- The OG endpoint /og/postmortems/<id>.png does not exist yet (it currently falls through to 200.html, text/html); pages emit the API's og_image URL regardless, as the task asked.
- No live pypistats/GitHub data and github.com/divysinghvi answers 403 in this sandbox, so the uptime screenshots show 'Degraded — 1 of 3 monitored targets down' (github-profile http 403) and the self-api day is orange (its first probe ran before the server was up). Outside the sandbox the profile answers 200.
- Uptime shows the 90-day daily view only; the draft's hourly cells above 1024 px (K-I9, ?days=7&bucket=1h) were not built (task asks for one cell per day). Recorded in DEVIATIONS.
- No axe scan / Lighthouse run in this step (draft R.9 gates belong to a later step); no vitest units for lib/postmortem or lib/uptime (Playwright covers the behaviour).
- The severity-definition texts (SEV1–SEV4) are UI vocabulary in web/src/lib/postmortem/index.ts because the API exposes only the level; recorded in DEVIATIONS.
- Contact clipboard copy relies on navigator.clipboard (secure context); on plain http without permissions the button silently leaves the selectable text (tested with granted permissions only).

### notes_for_next

- Routes: web/src/routes/(site)/postmortems/{+page.server.ts (loads services for colours), +page.svelte}, (site)/postmortems/[id]/{+page.server.ts (entries from api.content.postmortems(); load → {pm: ContentPostmortem, services}; ApiError 404 → error(404)), +page.svelte}, (site)/contact/{+page.server.ts (build-time healthz snapshot or null), +page.svelte}, (site)/uptime/{+page.server.ts ({targets: UptimeTargetView[], snapshot: UptimeHeartbeats|null}), +page.svelte}. All three read data.profile/data.postmortems/data.siteOrigin from the (site) layout.
- lib/postmortem/index.ts exports SEVERITIES/severityDef(level) {level, tone red|orange|yellow|blue, definition}, formatContentDate(raw) (YYYY|YYYY-MM|YYYY-MM-DD → '2024'|'May 2024'|'14 May 2024', TODO verbatim), dateSortKey, sortNewestFirst (dated desc, then undated by id desc), byIdAsc, neighbours(items,id) {prev,next} in id order, spanTraceHref(spanId) = /trace/career?span=<id>. Components: SeverityBadge.svelte {severity, size sm|md|lg, definitions} (button named 'Severity SEVn' + role=tooltip, Esc closes), Toc.svelte {entries: TOCEntry[], active} (sidebar ≥900px, <details> below; nav aria-label='Contents', active link aria-current='location'), TodoBadge.svelte {value, title} — reusable anywhere a TODO(divy) must render literally.
- lib/uptime/model.ts exports dayCells(buckets, days, nowMs) → DayCell{key,ts,bucket|null} oldest-first ending on the UTC day of nowMs, cellState (up ≥0.9995 | down ≤0 | partial | none), cellTitle, summarizeCells, formatPct(null → '—'), formatLatency, formatSeconds, formatAgo(iso, nowMs), formatUtc, ERROR_CLASSES/errorClass/errorMessage ('<class>: <msg>' split), overallStatus(targets) → {level ok|degraded|partial|unknown|none, text, up, down, unknown, unconfigured, monitored}, contentFor(views, id). Components: HeartbeatBar {cells, name} (role=img aria-label '<n>-day heartbeat for …', data-cells, .cell.cell-{up|partial|down|none}[data-day]), IncidentTable {incidents, name}, TargetCard {target: HeartbeatTarget, view?: UptimeTargetView, days, nowMs} (section[data-target][data-status], [data-note], role=alert on down with the error class chip).
- Test hooks: [data-postmortem-id] rows/article, .doc h2[id] sections, nav[aria-label=Contents], nav[aria-label='Other postmortems'] a[rel=prev|next], link 'View in trace' href /trace/career?span=<id>; contact: table caption 'Escalation steps in order', [data-channel=<key>], [data-open-to] .chip, [data-curl], [data-healthz-live=true|false], [data-clock] time; uptime: [data-overall=<level>], [data-collector-state], [data-target=<id>][data-status], .status chip text up|down|no probe yet|unconfigured.
- TraceViewer.svelte (minimal edit): spanFromUrl(hash, search) reads #span= / #trace?span= first, then ?span=; applyHash(hash, search) runs on mount, hashchange and page.url changes; writeHash strips `span` from the query so the selection lives only in the hash afterwards (URL becomes /trace/career#span=<id>). /#trace?span= from the API's span_url still works.
- types.gen.ts was regenerated here (W2's local PromResultType alias in web/src/lib/panels/prom.ts still compiles; it can now import PromResultType/PromResult from types.gen). `make gen-check` should be clean unless the API lane changes models again.
- Uptime refresh: REFRESH_MS=60000 and DAYS=90 constants in (site)/uptime/+page.svelte; refresh() calls api.uptime.heartbeats(90,'1d') then fetchReadyz(true) from $lib/panels/model (collectorStatus('uptime', readyz) drives the [data-collector-state] line). The page never invents probes: hb null → targets synthesised from content views as unknown/unconfigured.
- API lane: internal/server static handler should answer 404 for a missing `<route>/__data.json` (it serves 200.html now → client shows 500 for unknown ids); the OG route /og/postmortems/{id}.png is still unimplemented (pages already link it); the contact page shows X-Divy-Trace-Id when the OTel middleware lands (reads the header from the live /healthz fetch).
- Screenshot script: PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers SHOTS_BASE_URL=http://127.0.0.1:5173 SHOTS_DIR=<dir> node web/tests/shots/w3.mjs (locale en-US pinned like W2). Specs need the API on :8080 with probes (run `divy collect --once --db … --content ./content` BEFORE starting serve, then POST /api/collect?force=1 with DIVY_COLLECT_TOKEN so self-api is up; per-target due check (interval−10s) means force does not re-probe within 5 min).
- Process hygiene: /tmp/divy-w3 and /tmp/divy-w3-dist binaries remain (harmless); /tmp/divy-web-w3.db deleted; :8080, :5173, :18085 released. `ss` is not installed in the sandbox — kill by /proc argv (a `pkill -f` pattern matches the calling shell).

